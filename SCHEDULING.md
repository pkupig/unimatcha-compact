# 匹配调度与活动发布

> 怎么控制"什么时候匹配、什么时候公布"，以及活动/公告怎么发。
> 面向没有软件经验的读者，尽量白话。相关待办见 `BACKLOG.md`。

---

## 0. 先懂三个概念（白话）

- **cron 表达式**：一个"重复闹钟"字符串，格式 `分 时 日 月 周几`。例：
  | 想要 | 写法 | 备注 |
  |---|---|---|
  | 每周五 17:00 | `0 17 * * 5` | `5`=周五（**当前默认就是这个**） |
  | 每周日 20:00 | `0 20 * * 0` | `0`=周日 |
  | 每周五 18:00 | `0 18 * * 5` | |
- **定时器（scheduler）**：后端有个 `MatchScheduler`，它读数据库里的 cron，到点自动跑匹配（恋爱+交友都跑）。
- **配置表 `MatchConfig`**：数据库里一行设置——`cronExpr`（何时跑）、`isEnabled`（开/关）、`timezone`（时区）。管理员改它，定时器**立即生效，不用重启**。

**当前现状**：配置是 `0 17 * * 5`（每周五 17:00，北京时间）→ 每周五 17:00 **算完立即公布**。

---

## 1. 方案 A（本次落地）：每周日公布

### 思路
现在的模型是"**算 = 公布，同一刻**"。方案 A 不改这个，只把这一刻挪到**周日 20:00**：
- 用户整周都能进池报名（池子一直开着）。
- 周日 20:00 定时器一响 → 自动算 + 公布，48h 确认倒计时开始。
- "**周五开始**"用一条**开场公告/推送**实现（"本轮开始，周日出结果"），不是一个匹配动作。

> 想在公布前**人工审一眼结果**再放出去？那需要"算"和"公布"分开——见 [方案 B](#3-方案-b待做真两阶段活动)。方案 A 是算完直接公布。

### 步骤 1：把 cron 改成周日 20:00

**推荐用管理端接口**（改完立即生效）。先拿管理员 token（登录管理后台），再调：

```bash
curl -X PUT "$API/admin/matching/config" \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "cronExpr": "0 20 * * 0",
    "isEnabled": true,
    "timezone": "Europe/London",
    "description": "每周日20:00公布本轮匹配结果"
  }'
```

或直接用脚本：`scripts/set-weekly-schedule.sh`（见文件内注释）。

> - `$API` 是后端地址（可能带 `/api` 前缀，如 `https://你的域名/api`）。
> - **时区**：用户主要在英国填 `Europe/London`；主要在国内填 `Asia/Shanghai`。（待确认，见 `BACKLOG.md` 第五节）
> - **数据库直改（备用，需重启后端才生效）**：
>   ```sql
>   UPDATE match_configs SET "cronExpr"='0 20 * * 0', timezone='Europe/London',
>     description='每周日20:00公布', "isEnabled"=true, "updatedAt"=NOW()
>   WHERE id='default-match-config';
>   ```
>   直改 DB 不会自动触发 `syncCronFromDB()`，要重启后端；所以**优先用上面的接口**。

### 步骤 2：验证

```bash
curl "$API/admin/matching/config" -H "Authorization: Bearer $ADMIN_TOKEN"
# 应看到 cronExpr = "0 20 * * 0"
```
下一个周日 20:00 会自动跑；也可随时 `POST /admin/matching/jobs/trigger` 手动跑一次测试，然后 `GET /admin/matching/results` 看结果。

### 步骤 3：开场 & 公布文案（周五发开场，周日发公布）

目前还**没有公告系统**（见方案 [活动发布](#4-活动公告发布待做)），所以这两条先**用推送通知或手动**发。文案：

**周五开场（恋爱）**
> 💘 本轮心动匹配开启啦！完善你的资料和问卷，周日晚 8 点公布你的专属匹配。越用心填，越准哦～

**周五开场（交友）**
> 👯 本轮找搭子开始！填好兴趣和偏好，周日晚 8 点揭晓和你合拍的人。

**周日公布（通用）**
> 🎉 本轮匹配结果已公布！快去看看谁和你最合拍，48 小时内双方确认就能开启对话～

### 方案 A 一句话
**改一个 cron（周日 20:00）+ 周五发条公告**，就实现"每周一轮、周日公布"。零代码。

---

## 2. 方案 A 覆盖不到的（所以有方案 B）

- 无法"周五就算好、藏着、周日才放"——A 是同刻算+公布。
- 无法在公布前人工审核/微调结果。
- "开始"只是公告，系统层面没有"轮次"这个实体，管理员看不到"本轮进行到哪一步"。

如果这些你都不需要，A 就够了。需要，就上 B。

---

## 3. 方案 B（待做）：真·两阶段"活动轮次"

给系统加一个**轮次（Round）**实体，把"开始 / 计算 / 公布"拆成三个时刻。对应 `BACKLOG.md` P1-5。

### 新增表 `MatchRound`
| 字段 | 含义 |
|---|---|
| `id` | 轮次 ID |
| `mode` | romantic / friend / both |
| `opensAt` | 周五：开放进池，发开场公告 |
| `matchAt` | 周日某时：后台**计算**（用户还看不到） |
| `publishAt` | 周日 20:00：**公布**，用户可见、开始 48h 倒计时 |
| `status` | `SCHEDULED`→`OPEN`→`MATCHING`→`COMPUTED`→`PUBLISHED`→`CLOSED` |

### 管理端要加的按钮
- 建轮次 / 设三个时间（或"每周模板"自动生成下一轮）
- 手动：**立即开始 / 立即计算 / 立即公布 / 取消本轮**
- 看板：本轮状态、进池人数、算出多少对（公布前只有管理员能看）

### 定时器改法
不再是"cron 一响就算+公布"，而是每分钟 tick 检查轮次：
- 到 `matchAt` 且状态 OPEN → 跑匹配、存起来、状态转 `COMPUTED`（**先不给用户看**）
- 到 `publishAt` 且状态 COMPUTED → 状态转 `PUBLISHED`，匹配变可见、发公布通知

> 计算和公布之间那段（COMPUTED），管理员可审核/剔除异常对，再公布——这就是 A 给不了的。

---

## 4. 活动/公告发布（待做）

目前后端**没有公告/活动模型**。要"控制活动发布"，需要新增一小块（`BACKLOG.md` P1-4）：

### 新增表 `Announcement`
| 字段 | 含义 |
|---|---|
| `title` / `content` | 标题 / 正文（支持富文本/图片链接） |
| `audience` | all / romantic / friend |
| `startAt` / `endAt` | 展示起止时间 |
| `isPublished` | 是否发布（草稿箱 vs 已发布） |
| `placement` | banner（首页横幅）/ popup（弹窗）/ push（推送） |

### 管理端接口
- `POST /admin/announcements` 建（默认草稿）
- `PUT /admin/announcements/:id` 改
- `POST /admin/announcements/:id/publish` 发布 / `.../unpublish` 下架
- `GET /admin/announcements` 列表

### 前端
- 首页读"当前已发布且在展示时间内"的公告 → 横幅/弹窗展示。

### 和轮次的关系
方案 B 的"周五开始"= 系统自动**开一轮 `MatchRound`** + **发一条 `Announcement`**。两者搭配就是完整的"活动化运营"。

---

## 5. 落地顺序建议

1. **现在**：方案 A（改 cron 到周日 + 手动/推送发开场与公布文案）。← 本次已给全
2. **上线后有余力**：活动/公告系统（P1-4）→ 让开场公告可视化管理。
3. **要仪式感/要审核**：方案 B 轮次（P1-5）。

> 提醒：调度、轮次、公告都是**后端 NestJS** 的活儿（这个工作区只有编译产物，没 TS 源码，所以 B 和公告系统我只给了设计，没直接改代码）。方案 A 是纯配置 + 文案，照上面做即可，不动任何代码。匹配算法（matching-ml）完全不受影响——它只管"给一批人算谁配谁"，何时算/何时公布是后端的事。
