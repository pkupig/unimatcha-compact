# CLAUDE.md

## 项目简介

Unimatcha —— 面向大学生的长期恋爱匹配平台（v2.0）。每周五 17:00 公布一轮匹配结果。Monorepo 结构：

- `apps/api` — NestJS 后端（Prisma + PostgreSQL + Redis/BullMQ）
- `apps/admin-web` — Next.js 管理后台
- `apps/h5` — H5 移动端（SPA）
- `apps/web` — Web 端
- `apps/website` — 官网（Aleo 风格，霓虹绿主题，双语）
- `apps/ios` — iOS 端
- `matching-ml` — 匹配模型服务（Python/FastAPI，规则+微调 LLM 混合，见 [matching-ml/README.md](matching-ml/README.md)）

项目待办总表见 [BACKLOG.md](BACKLOG.md)，**产品功能清单**按端分三份：[docs/FEATURES-APP.md](docs/FEATURES-APP.md) / [docs/FEATURES-WEBSITE.md](docs/FEATURES-WEBSITE.md) / [docs/FEATURES-ADMIN.md](docs/FEATURES-ADMIN.md)（纯功能列表，用户手动维护；技术视角底稿见 [docs/PROJECT-CHECKLIST.md](docs/PROJECT-CHECKLIST.md)），匹配调度方案见 [SCHEDULING.md](SCHEDULING.md)。详细架构、API 文档、快速开始见 [README.md](README.md)。

仓库说明：GitHub 远程 `origin`（https://github.com/pkupig/unimatcha-compact）是**唯一**项目仓库（2026-08-19 由 `pkupig/unimatcha` 迁来），推送用 `git push origin main`，主分支 `main`；部署另走服务器裸仓库 `server`（root@209.97.179.143:/opt/unimatcha.git），上线一律 **`git push origin main` + `git push server main`** 两条都推。旧仓库保留为只读备份远程 `unimatcha-old`（pkupig/unimatcha，内容与迁移时一致）；重复的 `unipia` 远程已删除（它与 unimatcha 是改名后的同一仓库，留着会推错地方）。**警告**：新仓库里的 `skeleton-blueprint` 分支是迁移前那份**函数体被掏空的骨架蓝图**（square.js 94 行、`function isOfficial(p);` 这种不合法 JS），仅作留档，**任何情况下都不要从它取代码**。

源码一律以 `apps/` 为准（根目录 api/h5/admin-web 部署快照已于 2026-07-13 移除，现已不存在）。

> 2026-08-14 更正：本段此前写的是「远程 `unipia`（pkupig/unipia）为唯一仓库，推送用 `git push unipia main`；旧远程 `origin`（campus-love）已弃用」，与实际不符——当前工作副本只有一个远程 `origin`，指向 `pkupig/unimatcha`（即用户确认的仓库地址），没有 `unipia` 远程，`origin` 也不是 campus-love。推测仓库在 7/13 之后改名/重建而本文档未同步。
>
> 本机 git 身份未配置（提交会直接失败），已按历史提交作者在**本仓库**设为 `Mohan Ding <dingmohan2004@hotmail.com>`（未加 `--global`，不影响其他项目）。

## 每日日志规则

每个工作日在下方「每日日志」中**追加一条当天的记录**（新条目放在最上面），格式如下：

```markdown
### YYYY-MM-DD
- 完成：今天做了什么（改了哪些模块/文件、修了什么 bug）
- 进行中：尚未完成的工作
- 待办 / 明日计划：下一步要做什么
```

要求：

1. 每天第一次开始工作时，先检查今天是否已有条目，没有则新建。
2. 当天结束或完成一个阶段性任务后，及时补充「完成」内容。
3. 日志条目按日期倒序排列（最新的在最上面）。
4. 条目过多时（超过约 30 条），把最旧的条目移到 `docs/DEVLOG-archive.md` 归档。

---

## 每日日志

### 2026-09-01
- 完成（匹配页第二轮打磨：真轨道横滑 + 下拉刷新移除 + 两态几何统一 + 框头固定）：用户四点反馈+一点补充。①**三视图改广场同款真轨道**（最大改动）：恋人/朋友不再共用一个 #match-content——拆成 #home-track 下三个独立面板（chat / home-match-romantic / home-match-friend，各自纵向滚动，#tab-match 改 overflow:hidden），跟手平移、两页同框、12px 页缝、两端 0.3 橡皮筋、≥70px 松手吸附、点分段同一条吸附动画；相邻匹配面板预热（缓存渲计划页，拖出来不是白板）；bindSquareSwipe 全套惯例照搬（方向判定后 preventDefault 掐竖滚、settle 绑 document、pagerWidth 零宽回退）。**连锁改造**：计划页内部 id 全换 class+data-mp（双面板 id 会撞，fillPlanBox/tick 改面板内 querySelector）、倒计时 tick 双面板各按 data-mode 走、loadPlanData 竞态令牌按模式分桶（预热与激活并发拉偏好，全局令牌互相作废）、couple.js 挂载点 fallback 指 romantic 面板、`.fixed.top-0 ~ main` 安全区补偿失配（面板被 track 包住不再是 main 兄弟）→ 各面板 padding 自带 var(--sat) **且只算一次**（顺带修掉上一轮 margin+padding 双倍下移——正是「上方距离不对」的真因，实测 titleY=64=顶栏底+8px 与设计稿一致）；nav 自动隐藏改绑 chat 面板（#tab-match 不滚了）。②**匹配面板下拉刷新移除**：attachPullToRefresh 加 opts.enabled 谓词（match 页限定 chat 视图），movers 只挂 chat 面板。③**两态统一**：标题恒 .mp-title 26px（去掉 searching 24px 特例）、副文案 .mp-sub 钳两行高（min-height 3.3em），实测 idle/searching 几何逐项相等（26px/64/46/359）。④**摘要框头固定**（用户补充）：.mp-box 拆 .mp-box-head（匹配偏好+编辑/锁定，固定）+ .mp-box-scroll（偏好内容，滚动）。⑤非计划态居中改滚动安全（content 纵向 auto margin：矮内容居中、超高顶对齐可滚——旧版 justify-center 超高会裁顶）。验证（375px mock 全断言）：轨道 0/-387/-774 三档、mid-drag -487 跟手且两页同框截图、橡皮筋 -810 弹回、小位移弹回、chat PTR 可用/匹配视图被 enabled 拦、框头 scrollTop 999 后 headY 不动、真 searching（锁定行+离开钮在场）与 idle 几何全等、matched/no_match 布局与溢出安全、vite build 过。**测法教训**：点「加入匹配池」后增强确认卡会 await 挂起——不点掉它就断言 searching 几何，比的是 idle 自己（第一轮就这么假绿了一次，重测才实锤）。
- 完成（🚀 上线 75f1b62）：双推（origin + server）→ 服务器重建 h5 容器。核验：容器内 js+css 与本地构建 MD5 逐字节一致（index-BeOwYdNS.js）、app/api 200（--resolve 127.0.0.1，8/31 排障惯例）、八容器 Up。本轮全部改动仅 H5 + 文档，api/db 零变更无迁移。
- 完成（🎨 匹配页 idle/searching 全面重设计，恋人+朋友四态）：用户给了 Claude Design 定稿（根目录 `UniMatcha新匹配页 ui.html`——自解包 bundle，12MB；**读法**：抽 `<script type="__bundler/template">` 的 JSON 就是画布源码，别硬啃整文件）+ 口头需求（朋友模式同款/左上角统一加号/偏好+设置并进一个只读框/框大小不变框内滚动/编辑弹既有偏好卡/补充信息直接展示原文）。落地：①**页面结构**（match.js renderPlanState 一套渲染 idle/searching×romantic/friend 四态）：标题+副文案 → **出血荧光绿倒计时卡**（不规则圆角手绘感、周历行「今天白片+公布日白标手绘圈（公布日取 getNextRevealDate 与倒计时同源）」、单行 48px 白描边大数字，tick 就地更新+跨天就地重渲周历）→ **只读摘要框 #mp-box**（flex:1 框内滚动隐滚条；2×2 偏好网格——朋友模式第三格换「兴趣优先」；匹配设置段=增强模式只读开关 .mp-toggle+副文案、补充信息原文软填充文本框，空态灰占位）→ 贴底 CTA（进池绿 r12+光晕 / searching 粉描边「离开匹配池」）；searching 时「编辑」换「🔒 匹配中锁定 · 离开后可修改」（点=toast，用户拍板）+内容压暗 .55、增强副文案按 lastEnhancedRound 显「本轮已生效 · N 能量」。布局走 `#home-match-view.match-plan`（id 作用域压工具类），matched/情侣/空态分支自动摘类回居中布局。②**匹配设置抽屉删除**：#match-settings-overlay 整块移除，增强开关/补充信息/重新填问卷（用户拍板进卡）以原样式并入 #filter-overlay 偏好卡；装载并入 loadPrefsForMode（竞态令牌+loadFailed 防空白覆盖，原 openMatchSettings 的守卫全数保留）、保存并入 saveFilterPrefs（PUT 带 extraMatchInfo、**不带增强字段**——后端只认 /matching/start 扣费路径）；retakeQuestionnaire 改收 filter-overlay。③左上角三视图统一加号（tune 入口删除）。④偏好缓存 S.matchPrefs 分桶进 state.js + cleanupUserState 必清（防换账号泄显）；保存成功后摘要框就地刷新。⑤i18n 补 5 键（标题/四句副文案）；框内动态值走 zh 三元+data-no-i18n；深色全套补齐（绿卡恒黑字、分隔线白 9%、mp-extra 深底）。**验证**（vite dev + fetch/EventSource 全 mock、375px）：中英双态四状态截图逐项对设计稿；编辑卡按模式显隐增强区、兴趣 chip 从 profile 渲染；保存链路 PUT 载荷字段逐一核对+框就地刷新；增强确认卡→start body {enhanced:true}/{cells:1}；锁定 toast；620px 矮屏框 119px 内滚 287px 且 CTA 恒可见；matched→居中布局还原；加号菜单匹配页可开；深色 computed 色值核对；vite build 过。**记档**：toggleLang 会整页重载（mock 需重注入）；预览窗格隐藏时截图是陈旧合成帧，分段高亮「没切」以 DOM classList 为准（老坑再确认）。
- 完成（上线前对抗复查 workflow 19 代理：16 findings 确认 15 / 驳回 1，去重后 9 项全部修复）：①【high】**下拉刷新 × 框内滚动冲突**——PTR 只查 `container.scrollTop<=0`，计划页把 #tab-match 钉死后真滚动容器变成 .mp-box，框滚到中部再往下拖会同时触发整页下拉、>70px 松手即刷新且框内位置归零；attachPullToRefresh 加 innerScrolled 守卫（沿途祖先有「可滚且未到顶」的容器即不启动，touchstart/touchmove 双闸），顺带给 bindHomeViewSwipe 挂 square 同款 horizLock（横滑时 PTR 指示器让位）。②【medium】**30s 轮询无条件整页重渲计划页**（滚动位置每拍归零 + 每拍多拉一次 /matching/preferences）——renderPlanState 加同态守卫：标记写在 mp-box 自己身上（`data-plan="mode:s|i"`，其它分支 innerHTML 覆盖即天然失效），同 mode 同态只 fillPlanBox 不重建不拉网（实测同节点保留、scrollTop 80 不动、零额外请求）。③【medium】**偏好加载失败仍可 Save**——共享控件残留的可能是另一模式的旧值，PUT 会写错模式；加 prefsLoadFailed 整卡拒存（toast 反馈；不动按钮 disabled——applyPanelReadonly 的 350ms 定时器会覆盖它，视觉禁用是假的）。④【medium】**换账号泄显**——lastEnhancedRound 与在途偏好响应都不随 cleanupUserState 清理；新增 resetMatchPlanState（标记归零 + seq 令牌作废在途响应）接入 cleanup。⑤【low】**周历公布日误标**——徽标按星期几定位，周五 17:00 后 reveal 已是下周五、徽标却圈在本周（已过）五格上与倒计时自相矛盾（每周约 55 小时必现）；改按日期差定位，reveal 不在本周整行不出徽标。⑥【low】**X 键/下拉关卡绕过 closeFilterSheet** 致摘要框增强显示陈旧至下一拍轮询——X 改走 closeFilterSheet、bindSheetDragClose 加可选 onClose 参数，且 toggleEnhance/updateFriendCells 改完即同步框（实测滑块拖到 3 副文案就地变「保底 3 位 · 3 能量」）。⑦i18n 删 4 孤儿键（Match Settings/两句旧副文案/小写 Matching in progress）。⑧12MB 设计稿进 .gitignore。驳回的 1 条（重填问卷跟错模式）前提不成立——偏好卡模式选项卡早已移除。复验：全修复项浏览器断言绿 + vite build 过。**测法教训**：横滑测试会真的切走视图，后续断言要先确认「屏上的框是谁的」——一度把守卫正确拦截误判成同步失效。
- 完成（两处收尾）：①偏好卡标题「偏好」→「编辑」并真居中——justify-between 下左右两键宽度不同（关闭图标 vs 保存胶囊），标题夹中间必偏左，改绝对定位 left-1/2 平移；文案由 openFilterSheet 按语言设置 + data-no-i18n（'Edit' 太短，不进全局词典防误翻用户内容，8/13 教训）。②主页三视图（聊天/恋人/朋友）左右滑切换——bindHomeViewSwipe 挂 #tab-match，按当前下标 ±1 两端夹住（8/19 广场横滑教训）；只在松手判定不做跟手（三视图是显隐不是滑轨）；守卫：多指作废、.overlay.active（聊天对话/偏好卡）或加号菜单打开不切、|dx|≥60 且 ≥1.5|dy|（不抢纵向滚动/下拉刷新）。浏览器合成 TouchEvent 断言 9/9：三向切换+两端夹住+纵向/小位移/弹层守卫全过、标题几何中心 188==vw/2。

### 2026-08-31
- 完成（邮件全链路接入：注册邮箱验证码【新功能】+ 学生认证验证码改真实发送，待验收/上线）：用户指令「把 email 接好——注册验证码认证（原本没有）+ 学生认证邮箱验证码」。**基础设施**：新建 `apps/api/src/mail/{mail.module,mail.service}`——通用 SMTP MailService（nodemailer 9；env `MAIL_HOST/PORT/SECURE/USER/PASS/FROM`；三必填不齐 `isConfigured=false`；`MAIL_SECURE` 留空按端口推导 465→TLS/587→STARTTLS，**不写死默认**；构造函数 `transporter.verify()` 启动探活只记日志不阻断；连接/握手/收发三超时防 SMTP 挂起拖死请求；发送失败抛 503 不漏 SMTP 细节）。生产走 Spacemail（域名 MX 已指 spacemail.com），docker-compose api 加 `MAIL_*` 透传、DEPLOY.md 补 SMTP 段。**注册验证码（新）**：schema 新表 `EmailVerificationCode`（email+purpose 唯一、attempts 计数）；新端点 `POST /auth/register/send-code`（@Public + 复用 PublicRateLimitGuard）；`register()` 现要求 6 位 code。**学生认证**：`sendVerificationCode` 从「只写日志+返回 devCode」改为配了 SMTP 就真发（不回 devCode）。两处都：随机码用 `crypto.randomInt`、10 分钟有效、60s 重发冷却（按 `expiresAt-TTL` 反推签发时刻，不受 attempts 更新干扰）、email 统一 `trim+toLowerCase`（login 加小写回退兼容存量）、发送失败清码避免冷却把用户卡死。**H5**：注册表单加验证码行（`register-code` + `Send code` 按钮 + hint）、`auth.js` 加 `sendRegisterCode`+`doRegister` 带 code、`core.js` 加 `codeCooldown` 60s 倒计时（显示「Ns」语言中立、归零由词典观察器翻回）、`profile.js` 学生认证发码也接倒计时、深色补 `.dark .btn-secondary`（原写死黑字黑边深底不可见）。**iOS**：AuthService/AuthViewModel/RegisterFormView 加发码步骤+code 字段（静态类型审查，本机无 Swift 工具链）。
- **两轮对抗性复查（workflow 55+ 代理）挖出并修复的高危**：①`PublicRateLimitGuard` 取 `X-Forwarded-For` 最左值 = 客户端可伪造 → 换任意 XFF 即绕过限流，套在真实发信端点上 = 邮件轰炸+枚举（改为直接用 `req.ip`，main.ts 已 trust proxy=1，Caddy append 真实 IP 到末尾）；②devCode 回退无 `NODE_ENV` 门 → 生产漏配一个 `MAIL_*` 就把验证码随 HTTP 响应直出、邮箱校验静默归零（加 `devFallbackAllowed`，生产未配 SMTP 直接 503 fail-loud + 清码）；③`register()` 的「读 attempts→判>=5→increment」非原子 + `/auth/register` 无限流 → 并发批量猜码绕过 5 次上限（改原子条件 `updateMany(where attempts<5)` 占位 + 占位后重读比码 + register 也加限流 + `user.create` 包 P2002→409）；④学生认证 `submitVerification` 比码同样无上限（已登录用户可无限试爆破他人学邮的码）→ 同款原子占位 + schema 加 `verifyCodeAttempts`。**验证**：api tsc 0 + jest 38/38（新增 12 条验证码用例：冷却/attempts 原子占位/生产 503/错码消耗名额/邮箱小写归一/P2002）；H5 375px 实测中英双态（发码空邮箱拦截、devCode hint、倒计时 59s→拦重复点、错码格式不发请求、正确 6 位→payload 带 code 跳 profile、真发送态无 devCode 显中文提示、倒计时归零 zh 态翻回「发验证码」、深色按钮 #eceae6 可读、375px 无溢出）；H5 vite build 过。**迁移**：新表 + `verifyCodeAttempts` 带默认值均为纯增量，`db push` 无需 `--accept-data-loss`。**运维**：生产 `.env` 必须补 `MAIL_HOST/PORT/USER/PASS`（否则注册/学生认证发码 503），依赖新增 `nodemailer@^9.0.6`+`@types/nodemailer`（api 容器随镜像重建）。
- 完成（Spacemail 凭据实测 + 生产配置）：用户提供 `donotreply@unimatcha.ai` 凭据。本地直连实测两层全过——SMTP `verify()` 鉴权通过、真实发信 250 queued，**IMAP(993) 回读收件箱确认送达**（零依赖 node tls 手写 IMAP 状态机，SEARCH SUBJECT 唯一标记命中）；生产 `/opt/unimatcha/.env` 已幂等追加 4 行 `MAIL_*`（密码只进服务器 env，不落文档/代码/git）。**第二轮复查（5 代理）再修两项**：①`submitVerification` 重读只取 verifyCode、email 匹配仍用占位前旧快照 → 自我竞态可给未验证邮箱盖章（重读一并取 schoolEmail 复核）；②10/min per-IP 限流对校园共享 NAT 出口不友好（核心用户全是校园 WiFi）→ 限流守卫改可配置 mixin，auth 验证码端点独立 30/min 桶（与 /public 10/min 分桶；防爆破真正靠每码 5 次上限，与 IP 限流独立）。复验 tsc 0 + jest 38/38 + H5 build。
- 完成（🚀 上线 8cbc9a7）：用户指令「上线吧」。合并主干（fast-forward）→ 双推 → 服务器重建 api/h5。核验：db push「in sync」（email_verification_codes 表 + users.verifyCodeAttempts 列已落生产库）、线上 H5 bundle 与本地构建 MD5 逐字节一致、api/h5 200、MailService 日志「SMTP configured … as donotreply@unimatcha.ai」。
- **⚠️ 上线后发现：DigitalOcean 封锁 droplet 出站 SMTP 端口（25/465/587 全 timeout，裸 IP 复测排除 DNS/本机防火墙；993/443 正常）**——Spacemail 直连在生产走不通（本机直连实测是通的，问题只在 DO 网络策略）。**已验证出路：主流事务邮件服务的 2525 端口从 droplet 全部可达**——MailService 全 env 驱动，换 relay 零代码改动。
- 完成（📧 邮件通路切 Brevo relay，生产全链路 8/8 全绿）：用户选 Brevo 并完成注册+域名认证（DNS 已实测就位：brevo-code TXT + brevo1/brevo2._domainkey CNAME；SPF 维持 spacemail 一条未动——Brevo 走 DKIM CNAME 认证不需改 SPF，**若日后要加 include 必须并进现有那条，两条 SPF 记录=permerror**）。服务器 .env 切 `smtp-relay.brevo.com:2525`（MAIL_SECURE 留空按端口推导→STARTTLS；**MAIL_FROM 必须显式设 donotreply@unimatcha.ai**——relay 的 MAIL_USER 是 b744e5001@smtp-brevo.com 这种技术账号，缺省 From 会用它）。重启 api 后启动探活即报 `SMTP connection verified`（复查轮加的 verify() 当场兑现价值）。**生产端到端 8/8**（脚本在 api 容器内跑，绕开 droplet 解析不了自家域名的怪癖；IMAP 用 Spacemail 密码收件）：send-code 200 无 devCode → 60s 冷却 400 → **真实邮件经 Brevo 送达 Spacemail 收件箱、IMAP 解 MIME 主题取码** → 错码 400 → 真实码注册 201+token → 已注册邮箱 409；测试号删除（验证码行 DELETE 0——注册时已被一次性消费，顺带实证该设计）。Brevo 免费档 300 封/天，验证码用量足够；SMTP key 只进服务器 .env。Spacemail 邮箱保留收信/IMAP 用途。
- 完成（🚨 DNS 事故排障与恢复）：用户报「打不开软件」（Safari 无法建立安全连接）。逐层排查：服务器/Caddy/证书全健康 → check-host 六国节点**全部解析失败** → 根因=**域名 NS 被切到 Spaceship 停放服务器 launch1/2.spaceship.net**（与 7/17 上线日同一坑）——起因是当天 Brevo/Spacemail 的 DNS 记录被加在了 **Spaceship 的 DNS 面板**（「DNS 记录(13)」），Spaceship 顺势接管 NS；其 zone 有邮件记录但没有 app/api 的 A 记录 → 网站全球性下线，而邮件侧「看起来正常」。用户切回 ns1-3.digitalocean.com 后又连环两坑：①Spaceship 自动开的 **DNSSEC DS 记录**残留在注册局（DO DNS 不支持 DNSSEC → 验证型解析器 SERVFAIL，也是 7/17 原坑），用户在面板关掉后消失；②**DO zone 从来没有邮件记录**（MX/SPF/brevo-code/DKIM 全缺，audit 实锤 A 记录全在而邮件记录 NXDOMAIN）→ 给用户开了 6 条记录清单（MX×2 spacemail 10/20、SPF、brevo-code、DKIM CNAME×2）在 DO 面板补齐。恢复过程用 check-host 多国节点跟踪（德/塞/土先 200，其余等坏 NS 的 3600s TTL 自清）。**教训入档：NS 指向 DO 时，一切 DNS 记录只能在 DO 面板改；Spaceship 的 DNS 标签是不生效的摆设且会抢 NS。**期间 Brevo 队列自动重试，恢复后验证码邮件正常送达（用户实测收到）。
- 完成（注册后两处 UX bug 修复）：用户截图报①资料页兴趣区**两个添加按钮**——renderSetupTags 尾部拼的「+ Add New」聚焦 chip 是 7/18 加显式 输入框+Add 键之前的残留，删除；②**新注册用户保存资料后整页空白**、底导无激活态、重启 App 才正常——saveProfile 走了 `showPage('page-home')+switchHomeView('chat')`，跳过了 `switchTab('match')` 负责的「显示 #tab-match 面板 + 底导激活态」（showPage 刚把全部 tab 面板 display:none），改为 `S.homeView='chat'; switchTab('match')`（问卷完成流程早已是此写法且注释记了坑，唯独 saveProfile 漏改）。浏览器 375px mock 全流程断言：兴趣区无 Add New、保存后 page-home active/tab-match block/底导 match 激活/chat 视图可见。
- 完成（🗄️ 数据库迁移体系：db push → migrate deploy）：用户指出 migrations 目录为空、生产靠启动时 db push 的隐患（无版本/无历史/无回滚；「静默改表」半句不准确——7/13 起无 --accept-data-loss，破坏性变更是拒绝启动而非静默执行，但那等于换一种事故）。落地：①基线 `20260831120000_init`（migrate diff --from-empty 生成，50 表/85 索引/55 外键，纯事务型 DDL 可原子回滚）+ migration_lock.toml；②生产库先做只读漂移核验（与 schema 零漂移，唯一差异=ensure-search-indexes 管的 11 个计划内 GIN 索引）；③Dockerfile CMD `db push`→`migrate deploy`；④**空库从零引导实测**（服务器上一次性 postgres + 现有镜像挂载新迁移目录：deploy→索引脚本 12/12→seed，51 表含 _prisma_migrations 全部就位）；⑤对抗复查（4 代理）抓到 README 还有 6 处教人 `db push --accept-data-loss` 的高危旧文档（会绕过迁移历史甚至删数据）——全部改指 migrate 流程，ensure-search-indexes/schema 过时注释同步刷新；⑥DEPLOY.md 6.5 节：改 schema 流程（migrate dev / 无库离线 diff——旧基准取 git HEAD 的 schema，前提是 schema 与迁移永远同 commit）、带迁移部署先 `compose run --rm --no-deps api migrate deploy` 预检（旧容器持续服务）、忘打基线的 P3009 crash-loop runbook（恢复=同一条 resolve 命令，DDL 原子回滚零半应用）、**永远不要对已进迁移管理的库跑 db push**、dev 库恢复后先 resolve 否则 migrate dev 要求 reset。生产切换序列：build → `migrate resolve --applied 20260831120000_init`（只写账不跑 SQL）→ up -d → 核验 _prisma_migrations 恰一行 finished。
- 完成（💾 备份体系，905386c+ 已上线并演练）：用户指出 Postgres 只落 docker 卷、单机、DEPLOY.md 无备份章节。落地：①`scripts/db-backup.sh`——pg_dump -Fc（MVCC 快照运行中一致）+ **uploads 卷 tar**（用户图片与库同级重要，90MB 实测）+ `.env` 副本（丢密钥=恢复出的站起不来），产物 600/目录 700、14 天轮转、空 dump 检测；rclone remote「offsite:」配置后自动双写异地并轮转，未配置则每次 stderr 大声警告；②cron `/etc/cron.d/unimatcha-backup` 每日 04:00 UTC；③`scripts/db-restore-drill.sh` 恢复演练——**当场实测**：最新 dump 恢复进一次性容器，users 12/square_posts 44/迁移账本与生产逐项吻合（没验证过的备份等于没有备份）；④DEPLOY.md 6.8：备份内容/异地配置（任意 S3 兼容 + rclone crypt 建议）/每月演练/整机灾难恢复 8 步 runbook。**用户决策：异地暂不配、先只本地**（B2/Spaces/R2 三选项都给过；本地备份挡误删库、挡不住整机没了——脚本每日警告兜着，DO Snapshots 建议也已给）。脚本坑记档：`[ -f ] && cmd` 在 set -e 下条件为假=整脚本打死，一律写 if。**复查（2 代理）再修 9 项（779293c）**：①半截产物污染「取最新」路径 → .part+mv 原子就位+trap；②异地无补传语义 → rclone copy 整目录幂等同步；③**演练每跑一次泄漏一个含全量生产数据的匿名卷**（postgres 镜像声明 VOLUME、rm 不带 -v）→ rm -f -v + trap EXIT，当天两个泄漏卷已清；④dump TOC 可解析断言（[ -s ] 挡不住截断）；⑤flock 并发锁；⑥pg_isready 连续两次（官方镜像首启临时实例假就绪）；⑦dump 新鲜度<48h 断言（唯一能发现 cron 静默停摆的机会）；⑧演练改演练库/生产库并排对照（+messages/matches）；⑨**DEPLOY.md 第 2 节 clone 地址还是 unipia（重定向到 8/19 冻结只读备份，灾难恢复照做=旧代码接新库）**→ 修真 + 补裸仓库/post-receive 创建命令 + runbook 补 rclone 重建/--clean --if-exists 防半恢复/末步重建备份链路。加固版重放：备份原子落盘、演练六项 drill/prod 逐项吻合（51 表/12 users/44 posts/24 messages/10 matches/迁移账本同）、零容器零卷残留。
- 完成（⚡ SSE 实时推送替代轮询主力 + 域名/上传收尾）：用户第二批基建清单（轮询打爆 DB/上传单机/单机架构/AB·BI·工单·EDM/域名硬编码）。**工程决策：SSE 而非 WebSocket**——单机 Nest 下穿 Caddy 零配置（Caddy 对 event-stream 自动 flush）、EventSource 自带断线重连、两端零新依赖；事件只做「失效通知」（type+matchId），客户端收到走既有 REST 拉取，不产生第二套数据形状。①api 新模块 src/realtime：`GET /realtime/stream?token=`（EventSource 带不了头→token 查询参数，@Public+手动 JWT verify+role 校验）；内存 Map<userId,Set<Response>>、25s 心跳防 NAT 掐、单用户 5 连接上限、close 清理；**水平扩展时把 emitToUser 换 Redis pub/sub 即可（切换点已注释）**。②接线两处漏斗全覆盖：chat.sendMessage/nudge→对端 `message` 事件；NotificationService.create/createMany→`notification` 事件（全站通知唯一漏斗）。③main.ts：**compression 必须按路径豁免 SSE**（否则 text/event-stream 被缓冲、事件永远推不出去）；uploads 静态补一年 immutable 缓存头（uuid 文件名）。④H5：startRealtime/stopRealtime 进 core.js，checkUserState/doRegister 双入口开启、cleanupUserState+401 路径关闭；SSE 在线时聊天轮询 5s→30s、通知 15s→60s 兜底（取模跳拍），掉线自动回原频率。**踩坑记档：往「cleanupUserState 里加一行」的补丁按首次出现匹配插错进了 401 处理器（同一行内容全文件多处出现），浏览器 mock 断言当场抓住登出不关 SSE**——多处出现的锚文本必须带更长上下文。⑤事实修正与定界：H5 API 地址本就按 hostname 推导（无硬编码）、api 源码域名字面量≈0、uploads **已在每日备份里**（当天上午的备份体系含 uploads 卷）；官网 52 处域名字面量是 OG/sitemap/mailto 这类必须绝对写的——解法是 DEPLOY.md 6.9「换域名手册」（验证过的 sed+五步清单）而非模板化。⑥其余入 BACKLOG P2-6~P2-9（Web Push 需 SW——**只挂 push 事件绝不加 fetch 缓存**，7/26 启动页缓存事故教训已写进条目；对象存储/架构拆分/工单 EDM 各带触发线；EDM 现成走 Brevo）。验证：api tsc 0+jest 38/38、H5 build、浏览器 mock EventSource 断言（URL/事件分发/3s 节流/清理/幂等重启）。
- 完成（SSE 复查 12 项修复）：对抗复查（2 代理）最有价值一击——**「NotificationService 是通知唯一漏斗」前提为假**（createNotification 生产代码零调用方！点赞/评论/匹配结果/解除/过期/里程碑/加好友共 10 处直写 prisma 绕过漏斗），不修则最常见通知随轮询降频从 15s 退化到 60s。修复：①matching（5 处事务内通知）emit 一律放**事务 await 之后**（事务内 emit=客户端提交前拉空）；square 评论/回复/点赞、couple 里程碑（事务改返 created 标志）逐处补 emit；couple「I love you」消息补 message 事件（原最坏 30s 回归）；energy 退款深嵌多调用方共享 tx 助手、事件罕见，**接受 60s 兜底并记档**。②SSE 鉴权补用户状态校验（封禁/删号即断，原可挂 7 天 token 长连接；EventSource 收 401 按规范不重连）。③markRead 补 read 事件（已读回执原被降频放大到最坏 90s）。④pollChatMessages busy 时置 pending 补拉（busy-drop 与 30s 兜底叠加的空窗）。⑤节流改 leading+trailing（纯 leading 会让 3s 窗口内最后一条状态陈旧）；notification 分支同款节流+refreshNotifications 重入守卫（finally 复位）；stopRealtime 清 trailing 定时器（防登出后补发无令牌请求）。⑥挤兑循环：第 6 连接起服务端先发 evicted 帧、客户端收到主动 close 停轮询（原每 ~3s 重连互挤不收敛）。⑦补丁工具课：**「在 X 函数里加一行」按短锚文本全文件首次匹配会插错位置**（stopRealtime 插进了 401 处理器而非 cleanupUserState，浏览器断言当场抓住）——多处出现的锚必须带长上下文。复验：tsc 0+jest 38/38（两 spec 补 RealtimeService mock）+H5 build。
- 完成（🚀 SSE 上线 596bb1c + 生产端到端 7/7）：api/h5 重建，RealtimeModule 就位；**容器内实弹端到端**（临时双测试用户+已确认朋友会话，测毕全删零残留）：双方 ready 帧 → A 发消息 B **毫秒级收到 message 帧** → 发送方不回环 → B 标记已读 A 即时收 read 帧；线上 H5 bundle 与本地 MD5 逐字节一致、SSE 路由经 Caddy 可达（坏 token 401 即断不重试）。轮询自此降为兜底：聊天 5s→30s、通知 15s→60s（SSE 掉线自动回原频率），单在线用户 DB 查询压力约降 6 倍。
- 排障记档：①服务器侧域名健康检查一律加 `--resolve <host>:443:127.0.0.1`（droplet 自身解析对缓存尾巴敏感）。②本机 Docker 假死（命令挂起、镜像拉不动），本地真实 DB 端到端未跑成，由生产实测补齐（已 8/8 全绿）。③本机 curl 默认走 `https_proxy=127.0.0.1:7890`（Clash），测直连必须用 openssl/裸 IP 或清代理变量——今天一度被它误导以为 GFW 拦截。

### 2026-08-30
- 完成（🚀 问卷 v2 全链路上线，ae26b9a；生产恋爱 v4=18 题/朋友 v5=14 题已激活）：用户指令「上 v2」。契约 `matching-ml/questionnaire/uspark_questionnaire.json` 落地——**范围决策**：年龄/性别/城市/学校/年级/性别偏好/年龄范围/签名/补充信息已由资料页+匹配偏好收集，不在问卷里重复问；**补 life_smoking_self 自述题**（契约只写了「对方吸烟你能否接受」没写「你吸不吸」，过滤对缺事实侧没法判）。①schema：Question 加 code/semantics/hardness/weight/target、QuestionOption 加 labelEn；四种题型的渲染/打分两端**本就存在（休眠代码）**，启用+加固而非新写。②打分/硬门 TS+Python 双实现逐条同步：filter 只进硬门不计分；complement 查表 [0.5,0.8,1.0,0.7,0.3]（差 2 最高）；题内权重组内加权平均；硬门新增 db_distance 跨城淘汰（双方城市已知才判）与 life_smoking×自述吸烟淘汰。③extractor：hard 自由题反转默认极性——「还有什么完全无法接受？」语境自带否定，裸名词回答（「吸烟」）词典给 neutral、「爱抽烟的」还会反转成 like，一律升格 flexibility=1 绝对底线（mock 词典唯一够得到一票否决档的通道）。④seed 按 type 各自幂等（ser_intent/soc_energy）+ 每侧单事务——顺序写库中途崩会留「恋爱 v2 朋友 v1」或「朋友零 active」的永久坏状态且被 `|| echo` 吞掉。⑤H5：选择题 data-value+answerChoiceEl（修 inline onclick 注入 + **data-value 补双引号转义**，escapeHtml 不转义引号）；选项 labelEn 双语；**SCALE 五档翻转为 1=非常不同意…5=非常同意**（v1 与 iOS 的描述一直是这个方向、H5 一直反着——v1 已停用不收新答案，此刻翻转不污染存量配对）；深色勾图标 text-black 必须放 bg-neon 父级（放子级被 .dark .text-black 刷白、绿底上消失）；**问卷墙软化**：仅 idle 用户整页拦截，在池/已匹配/关系中用户照常渲染+顶部重填横幅——否则 v2 激活瞬间在池用户连「离开匹配池」都点不到。⑥payload 补发 CandidateProfile.bio（签名+自我介绍）——ML 的 extractor 一直声明 bio 而 NestJS 从未发送，S8 的 bio 半边此前是死路。**流程**：测绘（4 代理）→ 实现 → 对抗性复查（17 代理，确认 11/驳回 0）→ 全部修复后上线。验证：api tsc 0+jest 26/26+seed 独立编译；ml 冒烟 10/10（新增 4 条 v2 用例）；validate.py 过；H5 375px 四题型中英/注入/多选/深色实测；生产核验：两 active 版本题型/语义/hard 题分布逐项对、**ml 容器内实测 must_same_city 跨城对被淘汰**（复查点名的冒烟断言）、H5 MD5 一致。**运维记档**：a) matching-ml 容器必须与 api 同批重建——生产走 AI 路径，旧 ml 镜像 pydantic 静默忽略新字段，硬门完全不生效且零报错（复查 high）；b) 回滚 = admin 重新发布 v1 两个版本；**裸镜像回滚走不通**（旧镜像 db push 拒删 v2 列、起不来）；c) v2 激活后全部老用户完成度归零、在池老用户以空答案参与打分（问卷分中性化），下轮 9/5 17:00 前有整周窗口重填；现有 8 用户多为演示号，未做站内通知。
- 完成（深色转暖压暗，13d5969 已上线）：用户反馈「不够黑且发冷偏蓝，要微信/OKX 那种暖黑」。原基底 #15151a 的 B 通道比 R/G 高 5，整套 token 同一个蓝调。按「更黑 + R≥G≥B 轻微暖调」重排 13 个深色专用 token 共 40 处（基底 #15151a→#121110、卡片 #1f1f26→#1c1b19、主文字 #ececf0→#eceae6 等；全部 hex 先核实只出现在 .dark 规则内才做的全局替换）。验证：各层 computed 色值全部 R≥G≥B，浅色零改动。
- 完成（深色模式补漏 + 播种图文修正，84d4b27 已上线）：用户四张深色截图报「优化一下 dark mode」，最刺眼的是聊天列表白底白字不可读。**根因**：tab 容器与 .page 的底色是自定义规则写死的 `#f9f9f9`，`.dark` 那批 `bg-*` 类覆盖够不着它们——容器保持浅色，而名字被 `.dark .text-on-surface` 刷成白色。补漏统一放 main.css 末尾（赢同权重先后序）：`.dark [id^="tab-"]/.page/#splash → #15151a`；顶栏分段胶囊白底写死 → 深色+浅描边，**`.dark .home-mode-seg` 会连带把 active 的黑字刷白，必须显式补回**；广场选中分段黑字 → 白字，**必须带 !important**——深色区的文字分级规则全带 !important，不带的话特异度再高也压不过（实测 (0,3,0) 输给带 !important 的 (0,2,0)）。文字卡米白底深色下有意保留（截图风格即强调色）。**播种图文修正**（用户截图点名活动帖那张黑白脚部剪影）：picsum 随机 seed 的图与文案严重不符，逐张 Read 后按 8/10 惯例「文案跟着图走」——活动图换 food-stall、「天空油画」实为图书馆书墙改题、「湖边跑步」实为森林雪山改徒步、「自习角落」图不可用换粉花改题、失物招领改纯文字。**教训**：播种时未逐张验图（8/10 明明记过这条惯例），随机 seed 必须先看图再写文案。验证：深色态聊天列表名字 #ececf0 可读/临时行淡荧光绿/分隔线白 9%、广场容器深选中白、浅色态逐项复核无回归；线上 js+css MD5 一致。**顺带清理**：bash 会话 $TMPDIR 未设导致临时文件全写进 /（映射到 D:\Git），含一个 demo token 与十来个提交信息草稿，已全部删除。
- 完成（纯文字卡荧光笔风格 + 广场全类型演示数据，08733f8 已上线）：①**文字卡重做**（用户给了小红书风截图）：米白底 + 左对齐特粗体 + 首段荧光绿划重点条（引号装饰按要求不要）。highlightMarkHtml 取第一小段（CJK ≤6 字/拉丁 ≤12 字符、遇标点截断）用 linear-gradient 铺底盖字形下部 1/3；**先切分原文再逐段 escapeHtml**——先拼 HTML 再切会把标签切坏。旧 pastelBg+font-cute 无调用点随之删除。**排障记档**：python heredoc 里 `\x00-\x7F` 被解释成真实控制字节写进了 JS（grep 报 binary file），改用 `charCodeAt(0) < 128` 判 ASCII；随后删 pastelBg 时按 `}` 找函数尾又被模板串里 `${h % 360}` 的花括号截胡切坏了邻函数——**按花括号定位函数边界在含模板字符串的代码里不可靠**，两处都已修复并语法检查通过。②**演示数据**（生产 Prisma 直写、幂等、与主账号同校 Warwick）：推荐流 4 条纯文字帖（展示新卡）+3 条图片帖；校园墙失物招领/匿名表白/组队；投票（approved + 3 张真实选票，冗余票数与 SquarePollVote 行一致）；免费活动（Event+关联帖，capacity 200）；**置顶页 3 条按序**（官方通知 0/活动 1/失物 2）；评论（含匿名）+点赞且计数与真实行严格一致。3 个演示账号密码为服务器上随机生成、验证后即删，未落任何文档。**验证走生产 API 而非只查库**（以演示账号登录实测）：pinned 恰 3 条按序；校园墙正确排除置顶普通帖、保留置顶活动帖（先虚惊一场：墙上出现「失物招领」，核对后是 8/7 那条未置顶的旧帖，规则无误）；匿名帖出 HMAC 化名；投票票数 [0,2,1,0]；线上 bundle MD5 一致。

### 2026-08-19
- 完成（校园墙右侧新增「置顶」页 + 后台调序，96fe288 已上线）：用户要「校园墙右边再开一页只放学生会置顶信息；推荐页不显示置顶；到校园墙置顶入口才从右边滑出来；学生会能调顺序；墙上不再出现置顶帖（活动除外）；置顶页字更小」。**后端**：schema 加 `pinnedOrder Int`（**单独一列而不是改写 pinnedAt**——pinnedAt 要保留「何时被置顶」这个事实，拿它兼作排序键等于让调序变成谎报时间）+ 复合索引；listCampusWall 排除置顶帖但**活动帖除外**（活动有时效、错过就没了）；新增 `GET /square/v2/pinned`（同校/未隐藏/已审核，按 pinnedOrder，不分页上限 50）与 `PUT /admin/square/pinned/order`（**传完整有序 id 列表**，与问卷题目排序同一契约：整表重排幂等，两人同时调只是后到者覆盖，不像「上移一格」会把顺序搅乱；列表必须恰好等于本校全部置顶帖，多一条少一条都拒）；pinPost 新置顶默认排最前（最小 order−1）。**H5** 翻页器两页泛化为三页（SQUARE_PAGES 单一真源）：置顶段**绝对定位挂在分段组右外侧、不占 flex 槽位**（b357d0a 修正：首版把它放进 flex 流，一出现就把居中的「推荐/校园墙」双双往左推，用户截图报的正是这个）——那两段位置因此与置顶显隐无关；与校园墙间距 12px（组内 gap 是 32px）故视觉上贴着校园墙、字号 10px vs 12px、**按基线对齐而非盒子对齐**（字号不同，盒底齐会让小字看着偏低；用零尺寸 inline-block 探针实测基线差 6px，padding-bottom 补到 9px）；**横滑改按当前页下标 ±1 并在两端夹住**（原来写死「dx<0 就是校园墙」，三页后会滑错方向甚至跨两页）；置顶页字号靠「作用域类 + 工具类」覆盖（0,2,0 恒压 0,1,0，与构建后工具类顺序无关）；置顶页不再逐张挂角标、隐藏发帖 FAB。**审计（24 代理）挖出三处会静默出错的地方，均已修**：①`squareReqSeqs` 写死两个键 → 'pinned' 取到 undefined，`++undefined = NaN` 而 `NaN !== NaN` 恒真 → **每次加载都在竞态守卫处静默早退，页面永远空白且零报错**；②`layoutSquareMasonry` 若照搬 SQUARE_PAGES 会把 'search' 踢出重排列表，搜索页空态在 1px 行网格里只占 1 行（8/19 踩过的坑）；③`pagerWidth` 元素在但宽度为 0 时不回退，三页偏移退化成只剩页间距。**后台** moderation 加「置顶排序」tab（上下移 + 一次性保存）。验证：api tsc 0 + jest 26/26、admin tsc 0；375px 实测轨道偏移 0/−375/−750、置顶段 0→42px、三页横滑状态机（含两端越界弹回）全对、字号 16 vs 13、墙上仍留活动置顶帖、中文态全译；生产库 `pinnedOrder|integer|0` 已落、线上 js+css MD5 一致、api/admin 200、八容器 Up。**顺带**：把本轮碰过的 10 个文件行尾归一化回 HEAD 的 LF——编辑时混入 CRLF 会让 square.js 显示成 4000 行整文件重写，淹没真实改动（实际只有 245 行）。
- 完成（匿名头像被裁 + 帖子详情页眉重排 + 滚动收起，9cf986e 已上线）：①**头像被裁的真因**——上一轮加的匿名 emoji 头像把字号写死 16px，而信息流卡片上的头像盒只有 **w-4(16px)**，emoji 正好撑满溢出，看着就缺一块；改为**按容器反推字号**（w-N × 4 × 0.62），评论行原来写死的 14/16 一并换掉。实测 16px 盒里 emoji 收到 14×10，不再溢出。②**页眉重排**：去掉「Post」标题 → 改放发帖人（头像+名字+学校/官方徽标），正文里那行同名信息随之删除不再重复；`px-6 → px-3` 与正文同一条左边线，两个图标键各外挪 6px（`-ml-1.5`/`-mr-1.5`）——**要对齐的是 24px 字形而不是 36px 盒子**，实测返回图标左 12px = 正文标题左 12px；匿名帖走同一套 postAuthorDisplay，页眉出的是化名+动物头像不漏真身；打开时先清空页眉作者，防加载期闪现上一帖的人。③**日期**从原作者行右侧挪到点赞/评论**同一行最右**（实测右 363 = 375−12，与正文右边线齐）。④**滚动收起**：下滑收页眉+页脚、上滑或近顶恢复，沿用底导那套（阈值 6px / 顶部 40px 恒显）；**首版有 bug 已修（586b5ff）**——只用 transform 平移不行：页眉页脚是 flex 子项，位置腾走了槽位还占着，顶部/底部各留一条白带（顶部那条还能透出下层页面），内容压根没往上补，等于白收起（实测收起前后 #pd-scroll 的 top/height 完全不变）。改为 **position:absolute 脱流 + #pd-scroll 补等高内边距**：显示时视觉不变（标题顶仍 84px），收起时内容真正铺满。页脚高度随回复条/图片预览变，用 CSS 变量 --pd-footer-h 承载，**在四个改变它的时点显式同步而不是用 ResizeObserver**——RO 回调挂在渲染步骤上，页面不合成帧时根本不触发（本机实测新建 RO 连首次回调都是 0 次），布局正确性不能押在它上面；实测 61→139→162→139→61 逐格吻合。**测层序前务必先禁用过渡**，否则 overlay 的 opacity 冻结在 0 会让 elementFromPoint 穿透到下层页面、得到假阴性。用 transform 平移而非 display:none——后者会让 flex 重新布局、内容跳一下；**点评论输入框强制恢复页脚**，否则收起态下点不到自己要用的输入框。验证（375px 生产构建）：滚动 0→200 收起、回滑 120 恢复、再下滑收起、回顶恢复、聚焦恢复；匿名帖页眉「🦩 Cozy Heron · Warwick」；标题距页眉 20px；线上 js+css 双文件 MD5 均一致、api 200。
- 完成（评论配图 + 评论输入区重排，b69cf19 已上线）：①**配图**——后端本来就收 imageUrl（SquarePostComment.imageUrl + CreateCommentDto 都早有），**一直是前端没做**，这次补齐：图片键 → 缩略图预览（带移除键）→ **上传推迟到点发送那一刻**（选了又不发不该在服务器留垃圾）；评论里渲染配图（≤160px）点开走既有全屏查看器；**只发图不打字也允许**（配图本身就是内容），两样都没有才拦。②**重排**：原来是三行堆叠（回复条/匿名胶囊/输入行），越加越挤——改成一行放下 `[图片][匿名]+输入胶囊+发送`，页脚 100+ → **61px**（实测 36+36+225+36）。匿名开关从带文字胶囊改图标键，但**状态不能只靠图标颜色**：开启时输入框占位符同步变「正在匿名评论…」——隐私状态必须一眼可见，发出去才发现没匿名就晚了。③**顺带加固**：发送并发守卫（上传要几秒，防连点发多条）、失败还原草稿（文字/图/匿名原样还回来）、换帖清掉待发图（原来只清匿名开关）、**评论图点击不把用户 URL 拼进 onclick 的 JS 串**——escapeHtml 不转义引号，URL 带单引号就能跑出来，改从 this 读已过 safeUrl 的 src（沿用 7/13 审计定的做法）。④隐私核对：上传接口文件名是 uuidv4、URL 不含上传者标识，故匿名评论配图不会经 URL 反推身份。验证（375px 生产构建）：只发图 content:'' + imageUrl、图+文+匿名三者同时正确提交、发完预览收起且匿名复位、点移除清空、空内容不发请求、**上传失败时不发请求且文字/图/匿名全部还原、发送键恢复可点**、中文态「写下你的评论…」↔「正在匿名评论…」、纯图评论不渲染空 `<p>`；线上 bundle MD5 一致、api 200。**排障记档**：测「图+文+匿名」时一度以为 imageUrl 丢了，实为测试脚本复用同一个 DataTransfer.files——handlePdImage 里 `e.target.value=''` 会把那个共享 FileList 清空，每次重建 DataTransfer 后即正常。
- 完成（广场匿名重做：逐条选择 + 化名跟随语言 + 匿名头像，**并修掉一个存量真漏洞**，e6da6cf 已上线）：用户口径「帖子可选匿名，发评论也可各自选匿名；匿名帖底下的评论不该自动匿名；化名要跟语言；要有随机头像」。①**逐条匿名**：SquarePostComment 加 anonymous 列（随启动 db push 已落生产库，实测 `anonymous|boolean|false`），getPost 从「帖子匿名→全楼脱敏」改为逐条判断；H5 评论页脚加匿名开关，**默认关、发完即复位、换帖也复位**（匿名是每次主动选的动作，不该被上一条悄悄延续）。②**自曝守卫**：匿名帖楼主在自己帖下的评论强制匿名——他实名说话会被「作者」标指认，等于把发帖时选的匿名当场作废；**通知文案按这条评论的最终匿名值出、不是 dto 原值**，否则楼主实名回复的通知会把他暴露给收件人（审计代理专门指出的坑）。③**修复化名可被离线反解（存量真漏洞，已写脚本实测复现「唯一命中」）**：原 funAlias/authorToken 是**无密钥** FNV-1a，输入 userId/postId 又都是公开值——真实 userId 在别处照常下发（非匿名帖的 authorUserId、评论的 user.id），拿一批 userId 离线重算就能精确反解匿名者是谁。逐条匿名上线会把这个洞从「只暴露楼主」扩大到「每个匿名评论者」，故一并修：改带密钥 HMAC-SHA256（ANON_ALIAS_SECRET，缺省回落 JWT_SECRET，**不新增必配项**）+ 以 postId 加盐 → 跨帖不可关联，一次自我暴露不再连坐此人全部匿名发言。**副作用**：存量匿名帖的显示化名会变一次，属预期。④**顺带堵两个旁路**：shapeComments 原样带出 userId/user.id（匿名评论会把真实 id 发给所有人）；关系网亲密度把匿名评论也计入（好友在我帖下匿名评论后，我关系网里那条线照样变粗，等于旁路泄露）。⑤**删除**「楼主化名's 好友备注」标签——它不做 viewer 隔离，把楼主私下给好友起的备注广播给所有访客；新模型下更糟：评论者主动勾了匿名，楼主却因自己设过备注一眼认出对方。**这是删掉一条已上线的产品行为，属产品决策，已告知用户。**⑥**化名跟随语言 + 头像**：后端只下发不可反推的 aliasSeed（仍留英文 nickname 供 iOS），名字由前端按语言渲染——化名**走不了全局词典**（观察器是整段文本精确匹配，用户内容又一律 data-no-i18n），这是必须下发 seed 的原因；中英词表下标一一对应，同一个人两种语言下是同一只动物；头像 = 该动物 emoji + seed 定的柔和底色，同 seed 恒等、零网络零静态资源。⑦后台评论列表补 anonymous 与「匿名」徽章（审核员仍看真名才能处置，但要知道用户侧是匿名显示的）。验证：api tsc 0 + **jest 26/26（新增 6 条匿名用例**：匿名评论只留化名、实名评论不再被帖子强制匿名、作者标只给楼主、楼中楼同样脱敏、带密钥且跨帖不同、同帖同人恒等——这套用例守的是隐私底线，改坏应立刻红）；admin tsc 0；H5 375px 实测中文态「暖心的白鹭/安静的小袋鼠」↔ 英文态同 seed「Cozy Heron/Quiet Quokka」**同一只动物同一底色**、实名评论「林小满」在匿名帖下保持实名、楼主匿名评论有作者标而他人没有、开关默认关→发 anonymous:true→复位→第二条 false；线上 bundle MD5 一致、api 200、admin 200、八容器 Up。
- 完成（搜索口径收窄：广场只搜帖子 / 聊天只搜已有联系人，6f03d52 已上线）：用户反馈「广场搜索出来的内容不对，只要搜帖子」+「chat 搜索只要搜已有联系人，不是搜索所有人」。**广场**：前端去掉 PEOPLE 区，**后端也一起收**——searchAll 的空查询与正常返回两处都去掉 users 键（形状统一为 `{query, posts}`），SquareService 随之不再注入 DiscoveryService、square.module 去掉 DiscoveryModule、Swagger 摘要同步改写；**DiscoveryService 本身必须保留**（/discovery/* 与 /users/search 兼容壳仍在用）。**聊天**：runFriendSearch 只过滤 S.sessions，不再调 /discovery/users（FIND PEOPLE）与 /discovery/suggestions（猜你认识），实测输入时**零网络请求**；占位符「Search chats, people, schools」→「Search your contacts」——原文案本身就在暗示能搜到所有人。顺带发现这其实是**回退到 5a 轮已定的口径**（addfriend.js 里那句「加新好友只留在 Add by QR 面板」的注释还在），8/13 的发现功能把它推翻了，这次改回去。**连带清理**：userResultRow/connectToUser/dismissSuggestion/reasonLabel/unwrapEnv 整簇约 100 行失去全部调用点，一并删除；state.js 的 squareSearchUsers 死字段、8 条孤儿 i18n 词条同删（PENDING/PARTNER/FRIEND 三条保留——没验证到确无使用方，宁可留几十字节）。**审计顺带挖出一个被本次收窄放大的老问题**：H5 拉会话列表一直不传 limit（后端默认 50 上限 100），以前搜不到还能靠全局搜人兜底，收窄后**第 51 个联系人会彻底搜不到**——改为 limit=100。另把广场搜索引导文案「Find posts, topics and people」改掉，它已名不副实。验证：api tsc 0 + jest 20/20、H5 五文件语法+生产构建；375px 实测——后端即使返回 users 前端也不渲染、聊天搜索按昵称/学校/消息内容三种命中且全程零 /discovery 请求、会话请求带 limit=100、中文态全译；线上 bundle MD5 与本地一致，八容器 Up，api 200。**收尾（5b698f0 已上线）**：用户指示「直接删掉隐私开关」，设置页的 privacy.searchable「允许他人搜到我」与 privacy.discoverable「把我推荐给可能认识的人」两行删除，settings.js 不再镜像这两个键、SETTING_FALLBACKS 的 discoverable 兜底特例随之移除、i18n 四条词条清理。**后端一行未动**：两个键仍在后端 DEFAULT_SETTINGS 里并继续对 /discovery/* 与 /users/search 生效；**用户已存的值也不会被冲掉**——toggleSetting 只 PUT 被点的那一个键（实测翻 showOnline 时 payload 仅 `{privacy:{showOnline:false}}`），这点是删之前专门验的。遗留状态记档：searchable 默认为 true 且现在**用户无法再自行关闭**，不过 /discovery/users 与 /users/search 已零客户端调用，暴露面仅剩直接调 API；若要彻底了结，可另起一轮把这两个孤儿端点连同 discovery 的搜人部分一并下线。
- 完成（仓库迁移：unimatcha → unimatcha-compact）：用户告知「换仓库了，现在这个是最新的」→ `pkupig/unimatcha-compact`。**核查结论：它不是最新代码，是骨架蓝图**——唯一那条提交 `7bd9085 initiate` 由 `aimi <2806191910@qq.com>` 今天 20:11 从另一台机器的旧副本（`/mnt/e/.../Desktop/unimatcha`）生成，其 README-SLIM.md 自述「具体函数体替换为 TODO……不是可直接运行的完整项目」；实测 square.js 1870→94 行、chat.js 1071→57 行，内容形如 `function isOfficial(p);` **连合法 JS 都不是**。与本地对比 **净删 84,469 行 / 新增 4,273 行**、600 文件→315 文件、两边**无共同祖先**。按 schema 特征字段定位快照约停在 **8/13~8/14**（有 SquareCommentLike、UserSuggestionDismiss，无 pinnedAt / SponsorEnergyLedger / studentId），即缺 8/11–8/19 的能量经济+admin 重设计整批与今天全部 H5 改动。**若当成来源接着开发等于丢掉整个实现**，故先向用户确认而非直接切换。用户拍板：把完整代码 force-push 过去让新仓库成为唯一真源。**执行受阻**：`git push compact` 返回 403（Permission denied to **MohanDing**），同一凭据推旧仓库正常——即新仓库未给本机身份写权限，需用户在GitHub 上把 MohanDing 加为协作者。用户加完协作者后一次执行完毕：①先把对方那条骨架提交在远端存成 `skeleton-blueprint` 分支（整个覆盖操作因此可逆，README-SLIM.md / FILE_INDEX.md 也留了档）→ ②`git push origin main --force` （`7bd9085...f78a843 (forced update)`）→ ③核对新仓库 head 与本地同为 f78a843、提交数 253=253、**文件树差异 0**。远程重排：`origin` 指向 unimatcha-compact，旧仓库降为只读备份 `unimatcha-old`，并**删掉重复的 `unipia` 远程**（实测它与 unimatcha 的 head 完全相同——是改名后的同一仓库的重定向，留着迟早推错地方；8/14 那条更正说「没有 unipia 远程」其实也不准，一直都在）。部署链路不受影响（走独立的 server 裸仓库），今日三笔改动线上照常。**遗留提醒**：那份骨架是从另一台机器的旧副本生成的，若 aimi 侧正基于它开发，那边同样缺 8/11 之后的全部改动，两条线需要对齐。
- 完成（发帖页眉被状态栏压住 → 顺藤摸出同类 7 处 + 投票选项可删，b71e124 已上线）：用户截图报「发帖页上面有问题」+「投票选项还要可以减」。起因那条根因很小：`#overlay-new-post` 的页眉少写一个 `shrink-0`——安全区规则按 `.overlay > header.shrink-0` 匹配，少一个类整条不命中，Cancel/Publish 就被灵动岛压住。**由此做了一轮全量审计**（多代理并行扫 5 个维度 + 逐条对抗复核，确认 16 条 / 驳回 6 条；驳回的 6 条里有 4 条是代理读的 HEAD 快照、而修复已在工作区，反倒等于给我的实现做了逐行复核）。**漏网的共同特征：贴屏幕顶、但要么是绝对定位、要么是写死数值，那套按类批量补偿的选择器一条都够不着**——①对方资料页返回键（absolute top-4 浮在封面上，是该页唯一关闭入口，被压住只能靠侧滑退出）；②启动页 Skip（absolute top-8，主屏 PWA 下点击还会被系统「点顶栏回顶」抢走）；③下拉刷新圆片（top:0 且 z 低于顶栏，顶栏按安全区加高后圆片全程藏在顶栏后面，真机上下拉「毫无反应」——**这条我自己的 DOM 普查也独立命中了**，圆片在 y=9）；④加号菜单卡（写死 top:56px 照 h-14 顶栏算的）；⑤toast（写死 top:16px，被灵动岛从中间挖空）；⑥广场 FAB 拖拽上边界（写死 70，能被拖进顶栏底下并持久化，之后点不到也拖不回来）。**还挖出一条反向误伤**：`#filter-overlay` 是贴【底部】的弹层，页眉却带 `sticky top-0` 被安全区规则命中，抓手条上方今天就已有 59px 白带（`.sticky.top-0` 只认类名不认位置）——去掉 `top-0` 即可（滚动容器是兄弟节点，那个 sticky 本来也不吸顶），并把 main.css 里那句已被证伪的「不会被误伤」注释改成明确警告。**刻意不做**：把选择器放宽成 `.overlay > header`——实测放宽会误伤居中弹窗（问卷卡片，标题上方多 87px 死白）与底部弹层（题目导航），`.overlay` 下全屏页/居中弹窗/底部抽屉三类几何位置**结构上不可分**，放宽是拿三处回归换一处方便。修法统一走仓库既有惯例：id 作用域规则写进 main.css，`top` 一律写两行（`top: 56px; top: calc(...)`）——**`top` 与 `height` 不同，声明失效时退化成 `auto` 会让 fixed 元素直接掉到页面底部，比原 bug 更糟**。投票选项：新增 removePollOptionInput，每行尾 × 键、只在多于下限 2 时出现，删除后其余值经参数透传不丢且 placeholder 重编号。验证（375px 生产构建 + 注入 59px 模拟灵动岛）：**23 个页面/弹层普查「最靠上的可交互元素」全部落在状态栏之下**（改前广场那颗圆片在 y=9）；发帖页眉 123px/Cancel y=79/Publish y=73、资料页返回键 y=75、加号卡贴顶栏下沿、toast y=75、圆片起点 59、底部弹层页眉 padding 回到 12px、FAB 拖进状态栏被夹到 112（顶栏底 104+8）、投票 2→4→删中间保留 A/C/D→点 × 减到 2 后删除键消失；线上 js+css 双文件与本地构建 MD5 逐字节一致。**顺带记一条未修的相邻缺陷**（已开独立任务）：viewPartnerProfile 先开弹层再 fetch，请求失败只 toast 不关弹层，此时页面空白既无返回键、又因 hasBackButton 探不到 arrow_back 使左滑返回一并失效，用户被卡在纯色全屏页只能重开 App。
- 完成（H5 三项界面：搜索/能量改全屏页 + 扫码行修真，77f3cd9 已上线）：①**广场搜索独立成页**——原为顶栏内嵌搜索条，撑高顶栏还要 syncSearchPadding 反复补内容区上边距；改为 `#square-search-overlay` 全屏页（左箭头 + 圆角输入 + Search 键），**搜索范围改跨两个板块**（不传 board，可见性由后端各自把关）。关键收益：结果渲染在搜索页**自己的瀑布流网格**里，推荐/校园墙信息流因此永远保持未过滤——原来「切页 = 清搜索」那套（switchSquareTab 里清关键词/清结果 + squareRenderedSearch 标记 + 切页重拉）连同 toggleSquareSearch/syncSearchPadding/点外收起监听全部删掉。层序按 DOM 顺序解决：搜索页在底导之后、详情页之前，同为 z-50 → 盖住底导、被详情页盖住（实测 elementsFromPoint 确认）。**踩坑记档**：搜索页的引导态/加载态/错误态写进 1px 行网格后只占 1 行、内容看不见，三处都要跟着调一次 layoutSquareMasonry（信息流的空态本来就是这么做的）。顺带把点赞同步扩到推荐/校园墙/搜索三份缓存、卡片原地补丁的选择器纳入搜索页网格（否则搜索页点赞返回信息流看到旧状态）。②**能量购买改全屏页**（原居中弹窗），页眉与其它全屏页统一，套餐/支付/确认逻辑一行未动。③两页都注册左滑返回。④**扫码加好友输入行**：Add 键原用 `.btn-cta`——它是 `display:block;width:100%` 的整行按钮，放进 flex 行后把输入框挤到近零宽、自身溢出屏幕右缘；改为独立样式的窄按钮。验证（375px **生产构建**）：跨板搜索返回 2 卡 + PEOPLE 区、卡间距 6.1px、清空回引导态、详情页盖在搜索页之上且关闭后搜索页仍在、两页左滑返回并清干净 inline 样式、能量页 3 套餐 3 支付 + 支付键完好、扫码行输入 251px/按钮 68px/右缘 351px 无溢出、中文态「搜索广场 / 搜索帖子、话题和同学 / 获取能量」全译；线上 bundle 与本地构建 MD5 逐字节一致（index-DkTWY7se.js），八容器 Up、site-stats 200。**环境备忘**：预览窗格不显示时页面不合成帧、CSS 过渡冻结在起始值（opacity 0/visibility hidden），量层序前必须注入 `*{transition:none}`，否则 elementFromPoint 全是假阴性。
- 完成（🚀 admin 重设计 + 能量经济全案上线：worktree 分支并主干后推送部署）：用户指令「上线吧」。**真合并**（分支 33 提交 × 主干 38 提交，主干一周内新增广场搜索/发现/评论点赞/详情重排/聊天重排/活动学生会专属/票夹票券化/封面铺状态栏）：9 文件冲突逐一调和——主干在旧单体 AdminController 上改的「活动仅学生会+SUPER」移植到 events-admin.controller，旧单体 square/events 路由块删除；events.service 采主干「活动帖恒发校园墙+必须带校」保留分支能量购票/取消退款/内容编辑/核销 claim；square.service 保留主干搜索全量 + 分支置顶排序/isPinned/后管拆除/三方法公开化；H5 square/profile 以主干重构为基底重放分支门票能量计价/购票能量校验流/置顶角标三卡/票夹实付行；admin 活动域适配新规则（板块选择器移除/SUPER 建活动学校必填/TEAM 隐藏发布按钮）；CLAUDE.md 双侧日志按日期归并。**中途踩两坑记档：①共享 node_modules 的 Prisma 客户端被主干会话按旧 schema 重新 generate（合并树 tsc 报 60+ 模型缺失），用合并后 schema 重新 generate+db push 即愈；②主干会话在合并期间又推两笔（封面铺状态栏），增量二次合并零冲突。**验证：api tsc+jest 20/20、admin tsc+lint 0+build、H5 build；三条守恒链回归 40+37+38 全绿（门票链 E6 全网活动用例改断 400——无校活动已被主干产品规则禁止；r6 链 setup 补清历史置顶保重放确定性）；广场搜索/发现接口合并后实测正常（搜「置顶公告」4 命中）。**远程考古**：GitHub 仓库已由 pkupig/unipia 改名 pkupig/unimatcha（旧 URL 重定向同 head），本地 origin 仍指最老的 campus-love——按 8-14 更正的仓库说明把 origin 重指 pkupig/unimatcha 后推送；server 裸仓库照旧。部署：push origin+server main → 服务器重建 api/admin/h5 容器（db push+seed 随启动自动跑）→ 手动跑 backfill-admin-role → 核对无在途 PENDING/APPROVED 旧提现与审核中旧广告单 → 五端点冒烟。

### 2026-08-14
- 完成（推送 + 仓库说明更正）：本地与 GitHub 对比为 `0 0`（远端无本地没有的提交），故**不存在需要取舍的合并**，8/13 三个提交纯 fast-forward 推上去（31a3eb6..c9645be），远端原有内容一行未被覆盖。两件记档：①**CLAUDE.md 原「仓库说明」与实际不符**——原文写「远程 `unipia`（pkupig/unipia）为唯一仓库，推送用 `git push unipia main`；旧远程 `origin`（campus-love）已弃用」，但当前工作副本只有一个远程 `origin` 指向 **pkupig/unimatcha**（用户确认的地址），既无 unipia 远程、origin 也不是 campus-love，推测 7/13 后仓库改名/重建而文档未同步，已更正（13d2f1d）。②**本机 git 身份未配置**（提交直接失败），按历史提交作者在本仓库（非 --global）设为 Mohan Ding。③清理：测试用容器当初取名 `unimatcha_postgres`/`unimatcha_redis` 与 docker-compose.yml 声明的 container_name **完全撞名**（配置却是 unimatcha/5433 vs compose 的 campuslove/5432），留着会让用户下次 `docker compose up` 直接报名字冲突——已删除，后续测试容器改用 `unimatcha_test_*` 前缀。
- 完成（P1-9 广场搜索纳入评论内容）：产品口径经用户拍板取「**返回主帖 + 标注命中片段**」而非定位楼层（后者同一帖多条命中会出多张卡，且要新增详情页滚动定位+高亮）。①索引：ensure-search-indexes 增 `idx_square_post_comments_content_trgm`（现 12 条全 OK）。②检索：`LEFT JOIN LATERAL` 每帖只取**一条最早**命中评论——既给展示片段，又**天然按帖去重**（否则一帖 10 条评论命中会用自己的评论把整页刷满）；命中权重 1.2，**弱于正文 1.8**（讨论区提到 ≠ 帖子本身讲这个）。③隐私：片段截断 120 字、**只下发正文不带评论作者**（匿名帖评论在详情页本就脱敏，搜索结果不该成为反推身份的旁路）；可见性完全沿用主帖 scope。④体验：**帖子本身已命中时不挂片段**——否则卡上同时出现标题命中与一句无关评论，反而看不懂为什么搜到它。⑤H5 新增 commentSnippetLine（荧光绿左边框 + COMMENT 标签），接进小卡与校园墙宽卡。**踩坑记档**：片段行最初把 `data-no-i18n` 加在整个 `<p>` 上以保护用户评论正文，结果连自己的「COMMENT」标签也被挡在词典外、中文态漏译成英文——改为 **data-no-i18n 只包评论正文、标签留在外层可译**。验证：干净库上 P1-9 后端 17/17（评论命中回主帖、多条命中只出一张卡、片段正确且不含作者、帖子自身命中不挂片段、标题命中排在评论命中之前、中文评论命中、**他校搜不到经评论命中的墙帖**、**删帖后不再经评论复活**、统一搜索端点同样带片段）+ 浏览器 7/7（两卡命中、COMMENT 行渲染、只一张卡带片段、中文态标签译为「评论」且正文不被误翻、无报错）；原有两套回归 34/34 + 34/34 全绿，四套合计 92 项。

### 2026-08-13
- 完成（广场搜索 + 联系人搜索 + 猜你认识/猜你喜欢，全栈落地并双层实测）：
  ①**修一个静默真 bug**——H5 早就在给 `/square/v2/recommend|campus-wall` 发 `&search=`，后端只读 page/limit/cursor 把它**整个忽略**：搜索框亮起荧光绿「筛选中」高亮，返回的却是未过滤信息流。两个端点补 `search` 参数并接入真实检索。
  ②**检索选型 pg_trgm 而非全文检索**：广场内容中英混排，Postgres 内置 FTS 对中文不分词（'simple' 把整句当一个 token），要中文分词得装 zhparser/pg_jieba，生产镜像成本高；trgm 按三元组做子串匹配，中英一视同仁，且能给 `ILIKE '%x%'` 加索引（btree 对前后通配完全无效，正是原 ILIKE 全表扫的原因）。新增幂等脚本 `prisma/ensure-search-indexes.ts`（扩展 + 10 个 GIN/复合索引），接入 Dockerfile 启动链；**脚本吞异常不阻断启动**，配套 `PrismaService.hasTrgm()` 探测一次并缓存，缺扩展时 SQL 里的 `similarity()` 分量整段去掉、降级为纯 ILIKE，而不是让搜索端点 500。
  ③**广场搜索**：新增 `GET /square/v2/search`（帖子 + 用户两组，用户组只在第 1 页返回）。相关性 标题命中 3.0 > 标签精确 2.6 > 正文 1.8 + trgm 模糊兜底；终排 `rel × (1+0.12·热度) × (1+0.1·新鲜度)`——两个加成都是 ≤1.3 的乘子，保证「标题精确命中的冷帖」永远压过「正文擦边的长热帖」。可见性与信息流严格同口径：校园墙的同校硬过滤在跨板 OR 时**各自带各自约束**，不会被 OR 掉；匿名帖脱敏与 metadata 剔除沿用 shapeCard。
  ④**联系人搜索**：新建 `src/discovery` 模块。`GET /discovery/users` 检索面从「昵称/学校」扩到 昵称/学校/专业/城市/标签/兴趣，加相关性排序与分页；连接码精确命中置顶（**刻意绕过 searchable**——对方把码给了你就是明确同意）。`/users/search` 保留为兼容壳，出参形状不变。
  ⑤**猜你认识（隐私口径是本功能的主设计，非附属）**：这是恋爱匹配平台，把「谁在用」暴露给熟人是真实伤害。因此 `privacy.discoverable` **默认关闭**，且**双向要求**——viewer 自己没打开就整个功能不可用（返回 `enabled:false` 引导开启，而不是给个空列表让人以为没人），候选方没打开也不会被推出去；**单侧打开产生零曝光**。另加 `privacy.searchable`（默认开）与 discoverable 分离：愿意被知道名字的人找到 ≠ 愿意被系统推给同校同学。排除集合含**已解除关系的人**（故意不带 dissolvedAt 过滤——把分手/绝交的两人再推到对方面前是明确伤害）。召回三路：二度好友（`3.0·log2(1+共同好友)`，对数抑制社交中心节点刷屏）/ 同校同年级同专业+兴趣重合 / 广场共现（共现 ≥2 才算，唯一不依赖学校的跨校口子）；原因码由后端下发、文案在前端出（中英切换不需要后端参与）。新表 `UserSuggestionDismiss`，忽略单向永久。
  ⑥**猜你喜欢**：由点赞/评论聚合 tag/作者/学校三张权重表（评论权重 2.5×点赞——打字比点一下贵得多），进程内 5 分钟 TTL 缓存 + 点赞/评论即时失效；个性化分量权重 0.45 **刻意压在热度 0.5 之下**，且行为 <3 次不做个性化——画像信号稀疏，权重过高会让冷启动用户看到一堆低质长尾帖。作者亲和剔除自己（否则用户赞自己的帖会让自己霸占自己首页）。
  ⑦**H5**：广场搜索结果顶部新增 PEOPLE 区（共享 `userResultRow` 组件，已是好友显示 FRIEND/PARTNER 而非 Add）；Friend Hub 搜索面板从「只搜会话」扩为 会话 + FIND PEOPLE + PEOPLE YOU MAY KNOW（带原因与忽略）；设置页加两个隐私开关并把后果写进副文案。顺手修 `getSettingValue` 缺键一律兜底 true 的问题——discoverable 缺键必须兜底 false，否则老用户打开设置看到的是「开」，与后端实际行为相反。
  验证：API tsc + jest 20/20 + H5 vite build 全过；**起真实 Postgres（含 pg_trgm）跑两层实测，干净库上各 34/34 全绿**——后端 34 项（search 参数 bug 回归、标题优先于正文、中文关键词命中、校园墙他校搜不到、连接码置顶、searchable 开关双向、默认关闭/单侧打开零曝光/双方打开才出、忽略单向且落库、已加好友不再推、点赞 rowing 后 rowing 帖位次 4→0 而 knitting 3→7、匿名帖不泄露作者、未登录 401）；Playwright 375px 真机视口 34 项（搜索真过滤、PEOPLE 区显隐、切页清搜索与高亮熄灭、未开开关出引导而非空列表、推荐原因与忽略持久化、两个开关渲染、中英双态含推荐原因中文化）。排障两枚记档：**WSL 下 vite 对 /mnt/e（Windows 盘）的文件监听不可靠**，改了代码 dev server 仍发旧模块，验证前必须重启 vite——否则「测过了」是假的；`reasonLabel` 原用 `documentElement.lang` 判语言恒为 false（本项目切语言不写 html[lang]，只写 localStorage.cl_lang），中文态漏译成英文，改用 `window.getLang()`。
- 待办：本轮改动尚未提交（等推送指令）；生产首次部署需确认 DB 用户有 `CREATE EXTENSION` 权限（无权限则自动降级为无索引 ILIKE，功能不受影响但大数据量下变慢）；广场搜索目前只搜帖子标题/正文/标签，评论内容未纳入。

### 2026-08-12
- 完成（R7 学生会用户面收敛 + 手建广告商移除 + 存量高危泄漏修复，3f1951c/5b2d88b）：用户两项修正+一项追加（①学生会对本校用户只看整体不看个体，口径经确认：各年级人数/男女比例等群体信息+**流量与活跃度数据**+处理被举报用户即可，认证审核保留受限队列，重置匹配收归平台；②手建广告商仅移除学生会——正常渠道=邀请码自注册+无码平台直签，TEAM/SUPER 兜底与官网转开通保留）。workflow 三路实现+三视角复查。**探查顺带挖出存量高危：getUserDetail 用 include 全量吐 User 标量——passwordHash/有效期内邮箱验证码/connectCode/settings 对任何后台角色可见，已改显式 select 修死**。落地：学生会名单/详情/重置匹配 403；GET /admin/users/overview（总数/本周新增/年级/性别/认证四态/30 天注册曲线 + **活跃度**：发帖/评论/点赞/聊天按「用户×日」去重的 7/30 日活跃、日活曲线、活跃率，私聊只进去重不出条数；**内容流量**：广场发帖/评论/点赞三序列；匹配参与度 searching/relationship；raw SQL UNION+profiles join 按校过滤，学生会 school 参数忽略防越权）；GET /admin/users/verifications 受限队列（学生卡/校邮/昵称/年级，无登录 email，排序与提交时间列统一 updatedAt）；购票名单与处置动作响应 email 对学生会脱敏（复查抓到 PATCH 响应把 email 原路送回可枚举，MEDIUM）；shapeAdminPost 增 authorUserId + 举报队列「封禁作者」；/users 角色分叉（学生会两 tab 不发名单请求；SUPER/TEAM 名单零回归+认证审核 tab）；/accounts 学生会恒「生成邀请码」；AdminUserDetail 契约类型同步删除已不下发字段。复查 5 项（去重 4）全修。**验证**：38 项断言链一次全绿（403 矩阵/敏感五字段清除/概览越权参数忽略/dau7≤dau30/今日活跃与发帖≥1/队列受限字段/审核动作/购票双视角脱敏/建号三态）；门禁 api tsc+jest 20/20、admin tsc+lint 0+build；浏览器实测学生会 /users 全新形态四图两卡组真数据（总人数 5/活跃率 80%/年级性别分布）、认证审核空态、/accounts 生成邀请码按钮与启停保留。
- 完成（学生会扫码核销全栈落地，4ffc0b1）：用户需求「学生会要能扫码核销活动门票」。workflow 双路并行实现+三视角对抗复查。①**后端**：checkin 增可选 eventId 场次限定（活动门口误扫别场票直接拒绝不消费）；valid→used 改条件 claim——两核销员并发扫同票后到者必失败（扫码化后此竞态概率大增，原无条件 update 会双双显示成功）；claim 失败回读区分「已核销/活动取消已退款」；DTO 校验消息中文化。②**前端**：ScanCheckinModal（jsqr@1.4.0 纯 JS 逐帧解码，零原生 API 依赖 iPhone Safari 可用）——后摄取流 StrictMode 安全、HTTPS/权限/无摄像头/**中途失联（track ended）** 四态错误卡、**「离框重臂」去重语义**（码持续在框只触发一次不刷「已核销」红行；移开 ≥2s 重新出示才重发——截图复用已核销票会再次弹红警示，这是复查 MEDIUM 项的修复方案：在框存在性每帧刷新且置于 busy/暂停门之前）、限定活动下拉（limit 100）、结果流水（绿/红行+WebAudio 蜂鸣+震动）、torch 手电、非门票二维码本地预过滤；onDone 提前到 mounted 守卫前（关弹窗中成功也刷新父页）。③**验证**：三视角复查 7 项发现全修；假摄像头 E2E（隐藏面板不合成帧→captureStream/video 全废，最终用「原型层拦截 drawImage+ImageBitmap」注入二维码帧）七场景全绿：连扫两票→在框 7s 零重发→重出示警示→限定场次拒绝→非票码本地拦截（后端会答「票码不存在」，实测显示前端文案证明零 API 调用）→无摄像头错误卡→并发 claim；门禁 api tsc+jest 20/20、admin tsc+lint 0+build。**排障备忘：跨会话传长 base64 必须走文件写读通道，经控制台 stdout 会静默截断（症状=Image 能 load 但只渲染顶部若干扫描线，createImageBitmap 才报解码错）。** jsqr 依赖处置：主检出 --no-save 落包+worktree package.json 声明+--package-lock-only 同步锁文件（worktree admin-web node_modules 是实体目录非联接）。
- 完成（R6 三项新需求全栈落地，3a697aa/aa1df80/17edda7/60af00f）：口径经用户确认（帖子四项操作全做/活动开卖后有限编辑/可视化本轮只做广告商侧）。workflow 六路并行实现+三视角对抗复查。①**广告商禁发校园墙**（探查证实原前后端均无限制，真缺口）：后端 SPONSOR+CAMPUS_WALL 403，前端板块只渲染推荐流。②**学生会帖子/活动操作面**：置顶（SquarePost.pinnedAt 新列 db push；仅校园墙帖、学生会限本校；listCampusWall 置顶优先 nulls last；用户侧只出 isPinned 布尔；H5 大卡/中卡/文字卡三路 PINNED 角标）、编辑官方帖（非 SUPER 仅自己发布的；活动帖引导活动管理）、彻底删除 purge（物理删+DB 级联；活动帖拒绝）、评论管理（列表+删除，级联子回复，**commentCount 删后按实况重算**杜绝并发/孙级漂移）、活动内容编辑 PATCH :id/content（已售票票价锁定等值放行/容量只可上调 null=不限恒可/school 不可改注释固化/title·content·images 有变同事务**同步广场活动帖副本**；前端编辑弹窗 diff payload+票价禁用态+名额三态清空=不限）。③**广告商可视化**：详情页 CPM/CPC 预算进度条（超支变粉）+每日消耗 bar（能量轴）+分校对比行式（CSS 比例条避免曝光与能量混轴）；sponsorOverview 增 dailySeries30d（含 spendCents 逐日补零），仪表盘加 30 天消耗趋势卡。④**复查修复 5 项**：shapeAdminPost 漏发 postType（活动帖编辑/删除死入口，两位评审同报 MEDIUM）、容量改「不限」前端不可达（MEDIUM）、评论列表向学生会泄露跨校用户 email（脱敏为 null）、删评盲扣计数漂移（改实况重算）、详情弹窗头部计数不随删评刷新；代理还自主抓到 TeamDashboard 联合收窄字段因 30d 序列不再团队独有而失效。⑤**验证**：37 项断言链一次全绿（禁墙 403/置顶排序与越权矩阵/编辑权限矩阵/purge 级联/email 双视角脱敏/removed=2 计数/票价锁与等值放行/容量上下调/活动帖同步/取消态拒绝/30 天序列形状）；门禁 api tsc+jest 20/20、admin tsc+lint 0、h5 build；浏览器实测 moderation 按钮矩阵按帖型分层正确、置顶即时生效、评论区脱敏、活动编辑弹窗票价锁 300、广告详情两张 recharts 带数出图+预算 60%、H5 校园墙首卡=置顶帖带 PINNED 角标。
- 待办：用户浏览器人工验收（dev 三服务已起：API 3001/admin 3000/H5 3002；UCL 校园墙留有置顶示例帖）；学生会侧可视化（校园墙互动趋势/活动售票核销汇总/每场活动详情曲线）为下轮已确认范围；验收后合并 main 推 unipia+server（生产部署清单沿用 8-11 条目，schema 新增 pinnedAt 随 db push 自动落）。

### 2026-08-11
- 完成（能量经济 + 邀请注册 + 跨校合作全栈落地，worktree claude/admin-redesign-f13834 再 +6 提交，累计 19 提交待验收）：用户两组新需求（①广告商充值买能量 1元=100能量、广告全面能量计价、学生会分成收能量、两段式兑换赞助费再提现、学生会自设本校单价、平台可设全局+按校抽成；②学生会邀请码让商家自注册、商家↔外校学生会双向合作消息、拉入校低抽成）。四项关键决策经用户确认：mock 充值即时到账/提交审核时预扣冻结/两段式兑换/轻量消息线程。**关键洞察：1元=100分=100能量数值同构，全部 *Cents 字段原值承载能量，账本机制零改造只换计价面。**①Phase A schema：六新模型（SponsorEnergyLedger 商家钱包/SchoolCashLedgerEntry 现金账本/SchoolConversionRequest 兑换/SponsorInviteCode 邀请码/ContactThread+ContactMessage 双侧冗余未读计数）+School bps 改 nullable 三级继承（school override→SystemConfig ad_share_defaults→seed，复刻计价覆盖惯例）+seed 回填。②Phase B 后端五路并行：钱包（余额Σ账本/outstanding 净额幂等/mock 充值 clientToken 幂等）、广告状态机（提交 Serializable 预扣、approve 吸收 confirmPayment 直达排期写 paidAt、驳回/新增撤回按净额退款、settleCampaign 四小改加结余退款 charged+leftover≡budget 守恒+三级抽成，封顶数学零改动）、财务（computeBalance 冻结源换 PENDING 兑换/新 computeCashBalance/兑换 claim 幂等双账本对冲/提现改扣现金仅两行改）、定价抽成端点（学生会 PUT :id/ad-pricing 本校自设）、邀请码（等值乐观锁防超发+注册即登录 issueFor）、partners（唯一线程并入/消息事务未读双计数/平台只读监管）。③Phase C 前端六路并行：/wallet 钱包页、/register 公开注册页（Guard 外三态预校验）、ads 全面能量换装+余额不足引导+撤回动作+学生会定价 tab、earnings 四段化（能量/兑换/赞助费/提现）、finance 兑换审批 tab+报表兑换列、settings 全局抽成卡、accounts 邀请码 tab（链接复制/使用记录）、/partners 双页+60s 轮询未读角标；中途撞会话额度六代理全挂，按半成品盘点续作全部落地。④**验证**：38 项实弹守恒断言全绿（邀请注册→充值幂等→预扣→驳回退→撤回退→通过→注入消耗→挂起改期解除→结算：商家净支出 30,000=学校分成 9,000+平台留存 21,000→兑换冻结→审批对冲→绑卡提现→打款归零→洽谈未读/并入/越权 403/平台只读；首轮还意外实证了按校 50% 覆盖优先于全局的三级解析）；资金内核对抗复查抓到 **approve 无 claim 竞态真漏洞**（商家并发撤回可白跑广告+双退）等 5 项，全部修复后重放 38/38；两端 tsc/build/lint/jest 全绿。
- 完成（第二轮修正：开放注册 + 术语统一「广告商」+ 门票并入能量体系，再 +3 提交累计 22 提交待验收）：用户三项修正经口径确认（无码注册=平台直签、票款全额归学校、**用户侧暂保留「格」单位只接门票**——匹配定价重谈留待后议）。①**术语 sweep**：SPONSOR 角色名词「赞助商/商家」→「广告商」全站 71 文件 233 处（labels/permissions 单源改名 StatusBadge 侧栏自动跟随；钱侧「赞助费/赞助发放/Sponsored」刻意保留）。②**开放注册**：邀请码从准入门槛改为归因渠道——RegisterSponsorDto.code 选填，无码建号 sourcedBySchoolId=null（平台直签：团队审广告、直签档分成），有码路径逐字未动；/register 码改选填+两路说明卡、/login 加「注册广告商」入口。③**门票能量计费**：LedgerEntryType 增 EVENT_TICKET（±同型有符号）；EnergyService 最小泛化（mode|null+metadata+event_cancelled 通知，matching 零影响）；purchaseTicket 单事务=防超卖原样→consumeInTx 扣 cells=ceil(票价能量/100) 格（不足回滚不吞名额）→pricePaidCents=cells×100 进位快照→学校账 +cells×100（School 缺失 warn 归平台）；取消活动单事务 UPDATE…RETURNING 行级 claim+refundInTx dedupeKey=event-cancel:{ticketId} 幂等+学校负项冲回，used 票不退、学校余额可负（兑换侧校验挡住）；报表增门票净额列。前端：admin 活动域 Money→Energy 全换装；H5 购票重写（价格「N 格能量」双语/先刷能量条不足引导充值/确认卡明示消耗/成功刷新，票夹补实付行）。④**验证**：42 项门票+注册守恒断言一次全绿（2000 能量票扣 20 格→取消退款→重放幂等零新增/非整百 250→3 格快照 300/能量不足 400 且 ticketsSold 回滚/免费票零流水/容量 1 防超卖/限购 2/全网活动无学校账/核销后取消 used 不退且学校账保留/无码注册→平台审/重复邮箱 400）；两端 tsc+jest 20/20+lint+build（28 路由）+H5 vite build 全绿；浏览器实测登录注册两页入口文案、侧栏「广告商合作」、收益页真实门票流水四条（+2000/−2000/+300/+200 与脚本一致）、活动表能量计价、购票名单「200 能量/已核销」。
- 待办：用户浏览器人工验收（dev 已起 localhost:3000；测试账号 smoke.energy.*/ticket.smoke.*/openreg.*@test.local 系列）；验收后合并 main 推 unipia+server，生产部署清单：跑 seed（ad_share_defaults+bps 回填）、backfill-admin-role、**确认无在途 PENDING/APPROVED 旧提现与审核中旧广告单**（切换期语义，复查 ISSUE-3，生产广告未启用风险极低）；未来项追加：topup 条件唯一索引（接真支付网关前）、P2034 序列化冲突统一重试、购票 clientToken 幂等键（接真支付前）、Event.school 字符串弱关联并入 School 双标识归一、匹配定价重谈（届时决定是否 ×100 迁移用户侧并入统一能量）。

### 2026-08-10
- 完成（admin 全面重设计：后端九步收敛 + admin-web 推倒重写，worktree claude/admin-redesign-f13834，12 提交待验收合并）：用户反馈「admin 逻辑很乱」，三路深挖确认乱源（前端 12.1k 行 11 个超 500 行巨页/权限散 4 层互相打架/47 处 as any/Pager×6 复制；后端学生会范围校验 7 套实现/AdminController 大杂烩 8 领域/列表响应 7 种形状/dashboard 无 @Roles 等安全洞）后全案重做。①**后端结构收敛（不重写业务逻辑，9 独立提交每步 tsc+jest 门禁）**：新增 admin-core（AdminActor 规范身份 + AdminScopeService 唯一范围校验实现 + @CurrentAdmin）；认证主干修真——策略校验 isActive（停用即刻 401，关闭 8h 残留窗）、RolesGuard 归一化旧 isSuperAdmin、新增 GET /admin/auth/me；补三处缺失 @Roles（dashboard 原来 SPONSOR/role=null 可拿全平台统计，真修复）；configs 键白名单终结 ad_pricing_defaults 双写；Events/Square/Users 后管拆出到各域 <domain>-admin.controller.ts（逐字节搬移，URL 全不变，getAdminScope 类重复解析全灭）；契约统一（列表恒 {items,total,page,limit} 且 limit 封顶、dashboard/overview 字段名归一 adSpend30dCents/pendingReviewCount、余额单源 FinanceService.computeBalance、CurrentAdmin 内联类型×8→AdminActor）；admin 路径错误消息全中文化；backfill-admin-role.ts 幂等回填（开发库 0 行，生产部署时跑）。②**admin-web 全量重写（旧 src 11.5k 行删除，新 ~13k 行 170 文件）**：地基=typed fetch 客户端（弃 axios）/lib/types 按域拆文件单一导入面/labels.ts EnumMeta 枚举中文单源/permissions.ts 导航+守卫单一真源（菜单与权限结构上不可能不一致）/auth-context（/me 唯一身份真源+Guard 路由级守卫）/usePagedList（竞态防护全站标配）/useModal/18 件 ui 原语；12 路并行子代理逐域重写（每域先读后端源码核对契约），首次合体 tsc 零错误；新 IA=分组侧栏（总览/用户与内容/商业化/平台运营/系统）+角色差异文案+全角色可达账号设置（修 TEAM/SUPER 改不了密码的老缺陷）+深链 DEEP_LINKS 单源；IA 调整（用户拍板）：/sponsors 并入 /accounts、官方发帖移出团队/学生会顶级导航、用户反馈独立 /feedback 页；补齐后端有 UI 无的功能：财务手工调整 tab、问卷题目编辑+上下移排序。③**验证**：API tsc+jest 20/20×9 轮、admin-web tsc 0 错/next build 27 路由/eslint 零告警/grep 门禁（无 axios/无 barrel/无裸 hex/页面全 ≤150 行）；后端搬移保真对抗复查（SQL/状态机/事务体逐字节 diff 全一致，安全修复无绕过，零需改项）；**本机 Docker Desktop 恢复**（7/24 以来首次），起 postgres/redis+API dev+admin dev 实测四角色冒烟：SUPER 全侧栏/仪表盘真数据/финanс深链直落/投票审核匿名脱敏、学生会本校隔离（3/8 用户）/越权直链弹回/收益提现/现场建自拉商家、商家仪表盘/ads/new 锁本校+实时报价、401 伪 token 即刻踢回登录。
- 待办：前端对抗性复查代理收尾中；用户浏览器人工验收（dev 环境已起：localhost:3000）；验收后合并 main 推 unipia+server、生产跑 backfill-admin-role；未来项（本轮明确不做）：字符串状态列转枚举、操作人 ID 补外键、审计表、School 双标识归一、isSuperAdmin 列删除、银行信息加密。
- 完成（真实感演示数据：账号资料补全 + 广场 12 帖）：用户反馈 profile 下拉「看着没变」，生产实测证明**功能正常但无内容可展示**——`coverUrl` 为空，背景是 8×8 纯灰 SVG 占位块（放大纯色自然看不出变化）；同时实测下拉全程 hero 340→428→500px、背景下边界与白色功能区位移逐帧相等（88/88、160/160），锁定关系正确。据此补数据：抓 12 张真实摄影（picsum，12 种比例喂瀑布流）→ **逐张 Read 看图内容**再撰写文案保证图文一致 → 上传生产。①账号资料补全（原仅昵称/学校/年级/城市/性别/生日）：头像（湖边背影）、封面（意大利老城街景，细节丰富便于看出下拉放大）、bio、签名、专业 CS、MBTI INFP、国籍、兴趣 6 项、礼物罐 4 项、标签 5 项、照片墙 5 张。②广场 12 帖：推荐 8（书桌/户外写论文/徒步/湖边/杉树林/雪山/微距果实/猕猴）+ 校园墙 4（约伴冰岛带图/匿名表白/交换生活带图/农场博物馆）。验证：资料 11 项全 OK、推荐流 20 帖·校园墙 10 帖·21 张图全 https、封面与头像 HEAD 200。
- 完成（评论点赞 + 操作卡 + 详情页视觉调整，22145f4 已上线）：①**后端新增评论点赞**——新表 `SquareCommentLike`（commentId+userId 唯一）、`POST /square/v2/comments/:id/like` 切换点赞；计数按 `_count` 聚合不落字段（避免 likeCount 漂移）；帖子详情的 comments/replies 附带 likeCount + myLiked，出参 `shapeComments` 剥掉原始 likes 数组（否则把点赞者 id 泄露给所有人）。②**评论行加点赞按钮**（可直接点，就地刷新不整页重渲染、保住滚动位置）。③**评论长按改为弹操作卡**（分享/点赞/举报），不再直接进举报流程；点赞/回复按钮上不触发长按；举报仍保留两步确认防误触。④**页眉举报按钮换成「更多」**(more_horiz)，点开同款操作卡（分享/举报帖子）。⑤分享优先系统面板 `navigator.share`，不支持则复制剪贴板（AbortError=用户取消不报错）。⑥**楼主标记**由黑底 AUTHOR 徽标改为小圆点 + 轻字「作者/Author」。⑦详情页左右留白 px-6→px-4；**评论页脚底部留白改 `calc(safe-area + 14px)`**（原 `pb-safe` 在无安全区设备上为 0，输入条贴到屏幕最下沿）——用 `#post-detail-overlay > footer` id 选择器声明，不受构建后规则顺序影响。验证：生产端到端实测评论点赞（初始 0/false 且不含 likes 数组 → 点赞 1/true → 重读持久化 → 取消 0/false，测试数据已还原）；本地手机视口 17 项断言全过（点赞就地刷新、预点赞态粉色、长按弹卡且不直接举报、菜单点举报才两步确认、页眉更多卡、留白 16px、页脚 14px、more_horiz 图标、中英双态「分享/点赞/举报」「分享/举报帖子」）；新表已在生产库自动建好，线上 bundle 与本地构建 MD5 一致。
- 完成（帖子详情三段式重排 + 举报入口，16852e4 已上线）：①**修「滑动返回时页眉直接消失」根因**——`.overlay` 自身就是滚动容器（`overflow-y:auto`）而页眉是 `fixed`，滑动返回给 overlay 加 transform 后 fixed 子元素改为相对 overlay 定位，已滚动的距离把页眉顶到视口外。改为 flex 三段式（页眉/滚动区 #pd-scroll/页脚，均 shrink-0），overlay 用 **id 选择器**声明 `overflow:hidden`（不依赖工具类与自定义规则先后顺序，8/10 教训）。②**评论输入条改固定页脚**（原在内容流末尾要滚到底才可见）；评论数点击改为滚到评论区并聚焦。③**作者行（头像/名字/学校/时间）提到图片上方**。④**页眉举报按钮**：两步确认防误触（确认卡说明后果 → 填原因卡），走既有 `POST /square/v2/posts/:id/report`（后端已有去重+阈值自动隐藏）。⑤**评论长按 600ms 举报**：事件委托绑 #pd-content（只绑一次），移动 >10px 取消、桌面右键同效、震动反馈；后端无评论专用举报接口，走通用 `POST /reports`（category=content，内容带 commentId/postId/原因/正文摘要，管理后台「用户反馈」可定位）；入口快照 postId/摘要防确认期间状态被清。验证：手机视口真实渲染 21 项断言全过——overlay 不滚/内部滚、页眉滚动后仍贴顶且跟随 transform 位移、页脚贴底、作者行在图片前、两步取消均不发请求、走完两步正确提交（帖子 reason、评论 content 带 id 与摘要）、短按/移动不触发长按而静止 700ms 触发（父楼与回复楼均可）、中英双态文案全对（「举报这条帖子？/继续举报/举报原因」「华威大学/1 小时前/· 长按可举报」）；线上 bundle 与本地构建 MD5 逐字节一致。
- 完成（广场卡片信息精简，633964f 已上线）：用户反馈卡片显示太复杂。新增共享 cardAuthorRow（头像 w-5 + 昵称 + 点赞），推荐流三种卡（小卡/官方大卡/纯文字卡）右上角学校徽标去掉、底部时间去掉，统一为「标题 + 头像/昵称 + 点赞数」；校园墙卡仅去头部学校徽标，其余（头像/昵称/时间/正文/评论数/点赞）保持不变；帖子详情页学校徽标保留。用户内容（昵称/标题）补 data-no-i18n 防词典误翻。验证：线上 bundle 与本地构建 MD5 逐字节一致（9fc6cf25…），全包 school-badge 仅剩 1 处（详情页）；真实数据渲染实测双流卡片 school-badge 均为 0、小卡结构=标题+头像+昵称+点赞且无时间、匿名卡显示化名 Cozy Marten、校园墙卡保留评论数。
- 完成（广场行距生产事故真根因 + 滚动容器修正，9adba4a 已上线并生产实测）：用户电脑+手机全设备「还是一样」→ 服务器访问日志证实设备均已拉最新 bundle → **不是缓存，是 dev 与生产行为不一致**。登录态直测生产发现两个真根因：①**CSS 顺序**——构建后 Tailwind 工具类排在自定义规则之后，DOM 上的 gap-1.5 把 .square-feed-grid 的 row-gap:0 覆盖回 6px，1px 行网格每行再插 6px，span 数百行的卡片间距被放大成 984–2088px（span 本身全对）；dev 预览规则顺序相反故本地一直「验证通过」，7/26 时两值相同掩盖了顺序问题、8/7 改 0 后一上线即炸。修法：DOM 去 gap-1.5、列距由自定义规则 column-gap 声明，与顺序脱钩。②**滚动容器**——body overflow-hidden，window 不滚，真滚动容器是 #tab-square 本身；双页位置独立/重按回顶此前写在 document.scrollingElement 上真机全空操作，改为直接操作 #tab-square。生产登录态复测：12 帖间隙全部 6.3–6.9px、切页位置 500/180 各自精确恢复、rowGap 0/colGap 6px/autoRows 1px 全部生效。教训入档：**涉及与工具类同属性的自定义样式，必须在构建产物或生产上验证，dev 的 CSS 顺序不可信**。
- 完成（缓存加固追加）：用户冷启动仍拿旧版——线上 index 响应头确认 no-cache 正常、无 Service Worker，判定为设备侧顽固复用存储副本（iOS 主屏 PWA/微信内置浏览器已知行为）。nginx index/loaders/manifest 从 no-cache 改 **no-store**（客户端不再有旧副本可复用，0668478 已上线并验证响应头）；配合 8/10 版本漂移自愈双保险。
- 完成（两个「严重问题」排查 + 修复，均已上线）：①**广场三功能「失效」实为客户端跑旧 bundle**——真实生产数据+375px 视口复现证明 8/7 代码全部正常；用户手机缺的恰是 8/7 全部功能 → 旧 index.html 引用的旧 JS 命中一年期 immutable 缓存不 404、启动看门狗不触发的盲区。fix：看门狗补**版本漂移自愈**（ade6df0，启动后闲时 no-store 拉最新 index.html 比对资源哈希，不一致带参重载一次防循环；回前台 >30min 复查）；用户需最后一次手动强刷。②**聊天框 bug 多代理审计**（21 代理 5 维度 find + 逐项对抗复核，0 误报）确认 11 项并全修（fcb0ba5）：发送并发守卫/发送 A9 快照（防图片发错人）/游标不被己方消息推进（防对方消息永久丢失）+id 去重防重复气泡/乐观清空+失败恢复草稿/回车 preventDefault/closeChat 清 matchId/中文态占位符不再被打回英文/列表用户内容 data-no-i18n+预览按语言直出/聊天搜索 lastMessage 经 lastMsgText（原 [object Object] 搜索失效）/sendChatImage 补快照。③**聊天界面微信式重排**：列表去卡片改细线分隔+头像 48px+行尾时间；对话流头像贴气泡（36px）、每条消息时间戳改 ≥10min 居中时间条（同日 HH:MM/昨天/日期双语）、已读改气泡下「已读」小字；输入条 py-6→3+圆形箭头发送键；消息区留白收窄。④生产库聊天存量 http URL 改写（messages 0 行、聊天背景 1 行真中招）。验证：26 项浏览器断言全过（守卫/快照/游标/去重/回执/时间条/搜索/中英双态），线上 bundle 哈希一致、API 200。
- 完成（项目内容总清单）：新建 [docs/PROJECT-CHECKLIST.md](docs/PROJECT-CHECKLIST.md)——8 个并行代理全仓实扫生成的全项目功能盘点+待办主档：7 大模块 367 条 + 查漏 4 条，按 ✅已完成/🟡mock·留桩/🚧进行中/⬜待办/❓待确认 分状态并附代码路径依据（后端 API 52 条 81%、H5 75 条 93%、管理后台 49 条 88%、官网 47 条 94%、iOS+旧版 web 46 条 61%、matching-ml 40 条 83%、文档运维 58 条 50%）；查漏出 4 项边角（全仓无 CI、packages/shared 与 api metadata seed 双源数据漂移风险、排行榜后端 @deprecated 仍挂载、素材散件去留）；文末汇总跨模块待办 15 条。维护约定：markdown 为唯一主档（用户定的，不用 Excel/仪表盘），勾选/增删直接改文件。
- 完成（产品功能清单定稿）：格式经用户多轮收敛（Excel→可视化仪表盘→带状态产品清单→逐页纯列表→1038 行详细版→最终版），**定稿为按端拆分的三份纯功能列表**：[docs/FEATURES-APP.md](docs/FEATURES-APP.md)（16 个页面）/ [docs/FEATURES-WEBSITE.md](docs/FEATURES-WEBSITE.md)（7 页）/ [docs/FEATURES-ADMIN.md](docs/FEATURES-ADMIN.md)（16 模块）——只列功能名，无解释、无勾选框、无 emoji、无技术词，用户手动增删维护。中间产物 1038 行详细版（8 代理逐文件核实数字规则）随 PRODUCT-CHECKLIST.md 删除，内容保留在 git 历史（1457de7）可随时找回；PROJECT-CHECKLIST.md 为技术底稿（代码路径+完成状态）保留。按用户指示已提交并合并到 main。
### 2026-08-07
- 完成（广场演示数据 + 上传 URL 混合内容修复，已上线 f31d173）：①用用户账号（dingmohan2004@gmail.com，凭据仅经环境变量、未落盘）经生产 API 播种广场数据：本地零依赖生成 5 张异比例渐变 PNG（480×680/640×400/520×520/460×760/640×430）上传，发 10 帖——推荐 7（文字/图片混排喂瀑布流）+ 校园墙 3（失物招领带图/匿名表白/组队）。②过程中发现真 bug：Caddy 终止 TLS 后 Express 未设 trust proxy，/uploads/image 拼出 http:// URL，https 页面加载被按混合内容拦截（Safari 直接不显示）——main.ts 加 app.set('trust proxy',1)；生产库存量 http URL 批量改写 https（square_posts.images 9 行——其中 4 行是此前真实用户内容已中招，profiles 头像/封面/照片墙各 1 行）；重建 api。验证：新上传返回 https、双流 10 个图片 URL 全 https、抽样 HEAD 200、推荐流 12 帖/校园墙 5 帖。
- 完成（广场双页滚动独立 + 底导重按刷新）：①**滚动位置独立**——竖向滚动在 window 上、两页共用一个位置（用户反馈「切换时位置是同步的」）。switchSquareTab 切换前把离开页的 scrollY 存进 S.squareScrollPos，进入页即时恢复自己的位置；**切换不再无条件重拉**（此前每次切页都 loadSquareTab2 重渲染）——仅目标页为空或上次渲染是搜索结果（切页语义=清搜索）时才刷新；点赞/详情同步的 S.squarePosts 指针改由 S.squarePostsByTab 按页切换（needProfileSchool 分支同步维护）。进入广场时位置记忆清零。②**底导重按刷新**——switchTab 捕获 reTap（S.activeTab 已是 square）：平滑回顶 + 只 loadSquareTab2(当前页)，另一页内容与位置不动；首次进入仍双页预载。浏览器 mock 计数实测：进入双流各 1 拉；切页 0 重拉、推荐 400↔校园墙 150 位置各自恢复（矮内容时恢复值被 scrollHeight 钳制属物理上限）；重按导航仅当前页 +1；搜索后切走不拉、切回 +1 重拉未过滤流；vite build 过。**本日五项已上线（dc0dd61）**：推 unipia+server、重建 h5，线上 bundle 哈希与本地一致（index-C0wA4uAi.js，含 META_ZH/focusPdComposer）、API 重启后 200、八容器 Up。
- 完成（广场行距 bug 根修：dense 布局洞 + 重排失效两层）：用户报「每一行间距很远」。真实渲染路径复现出两层根因——①**结构层**：瀑布流靠 grid dense 自动回填，它按「最早可用行」而非「最短列」选位，两列不平衡时跨栏卡（校园墙/官方/广告卡）把长列旁的洞整段封死（复现 357px 大洞）；改为 JS 显式列/行定位：单列卡进较短列，跨栏卡压两列最大行，抬短列产生的洞记入 holes 由后续放得下的单列卡回填（视觉顺序微调换无大段留白，复现场景 357→119px 且剩口=塞不下最矮卡的几何必然）。②**时序层**：span 只在 layout 一瞬有效，此后图片/字体/翻译/宽度迟定都让它过期——scheduleMasonry 的 rAF 在页面不可见时永不触发（后台加载完图片回前台前 span 全错），加 250ms setTimeout 兜底；新增 ResizeObserver 盯每张卡（渲染后重挂、断开旧卡防泄漏、不观察容器防循环），任何后发尺寸变化自动重排；fonts.ready 后补一次。回归：纯小卡全 6.3px、校园墙全宽卡全 6.3px、混排洞回填生效；vite build 过。未提交（等推送指令）。
- 完成（profile 下拉背景模糊消散）：CSS 里早有「blur-mask 透明度由下拉手势控制」的设计意图但从未接线（共享下拉组件不暴露进度）。attachPullToRefresh 加可选第 4 参 opts.onPull(dist)——跟手每帧回调当前拉距、复位回调 0、刷新持住回调 THRESH（match/square 不传不受影响）；profile setupBgPullReveal 接入：拉距 0→140px 内模糊层透明度 1→0（跟手期间 transition:none 逐帧写值，松手还原空值走 CSS 0.45s 弹回）。合成 TouchEvent 实测七采样点：dist50→0.64/dist140→0/收回20→0.86/低于阈值松手→空值弹回/超阈值持住→0.5/刷新完→复位，内容 transform 复位不受影响；vite build 过。未提交（等推送指令）。
- 完成（帖子详情互动行三修）：①点赞/评论行下横线去掉（border-y→border-t，article 的 border-b 一并移除）；②留白收紧——article 下内边距 40→16px、行 py 20→12px、行上边距 32→24px、评论区顶距 32→20px；③评论数改可点击：详情页点评论数滚动到底部评论输入条并聚焦（focusPdComposer，smooth scrollIntoView+focus）；校园墙卡片上的评论数也可直接点（stopPropagation 后 openPostDetail(id, true)，详情渲染完自动跳评论条）。浏览器实测：无下横线/内边距全对/评论数为按钮/滚动定位到输入框；focus 在本机预览环境不可测（document 无焦点，环境限制），真机点击手势下为标准行为。vite build 过。未提交（等推送指令）。
- 完成（资料元数据中文显示映射）：编辑资料的学校/城市/专业/国籍等下拉在中文态仍显示英文——这些列表来自后端 metadata（英文原文）。新增 META_ZH 显示映射（i18n.js，234 项：大学 78/城市 50/专业 67/国籍 39 + 年级 4 档）+ window.metaLabel(v)：中文态查映射翻译显示，**值一律保持英文原文**（库/接口不变）；刻意不进全局词典，避免 London 之类短词误翻用户帖子正文。接线：fillMetaSelect 选项标签（编辑+注册两套下拉全覆盖）、下拉占位符补词典键（选择学校/城市/专业/MBTI/国籍/年级）、展示面 8 处（profile 页学校胶囊、公开资料卡 学校·年级、match 揭晓卡/朋友候选卡/关系卡学校+年级·年龄·城市行、广场 schoolBadge/校园墙卡/详情页徽标、活动块学校、聊天搜索副行）。浏览器 mock 双语实测：英文态原文不变；中文态五个下拉「牛津大学/伦敦/计算机科学/中国香港/大一新生」全译、value 仍英文、选中回填正确、广场徽标「华威大学」；vite build 过。未提交（等推送指令）。

### 2026-08-06
- 完成（通知界面视觉重排 + 编辑资料补性别/生日）：①**通知列表收紧**——行距 48→28px、组距 64→40px、图标盘 48→44px 圆角 12、未读粉点从行外挪到图标盘右上角、正文列表内钳制 2 行（详情看全文）、标题 15px、分组标签 11px 去缩进、Load More 改软填充胶囊；列表顶部留白收窄。②**编辑资料**——新增性别下拉（male/female/non_binary/other 对齐后端枚举）与生日 date 输入（16–40 岁区间同注册页，年龄提示随生日实时联动，保存时 age 按生日推算随 payload 提交；留空不覆盖已存值）；六个下拉去边框统一软填充样式、栅格间距收紧（gap-8→x4/y6）、整页节距收窄；顺手修保存键 bug——昵称为空早退时 btnBusy(true) 已执行、保存键永久卡禁用态，改为校验先行。③i18n 补 Non-binary=非二元/Select Gender=选择性别。浏览器 mock 实测：性别五选项、生日 min/max 正确、Age 22 联动、PUT payload 含 gender/birthday/age、越界生日拦截不发请求、保存键不卡；中文态通知重排样式全过（44px 盘/角标点/2 行钳制/加载更多胶囊）+ 性别下拉「选择性别/男/女/非二元/其他」全译；vite build 过。**已上线（c2ae83a）**：推 unipia+server、重建 h5，线上 bundle 哈希与本地一致（index-CW-Szhuv.js）、index 含 edit-gender、API 重启后 200、八容器 Up。
- 完成（通知面板中文化 + 头部靠右重排）：①**通知内容中文化**——后端通知为英文写死，H5 渲染时按语言本地化（notifications.js localizeNotif）：16 种标题映射 + 7 句静态正文 + 10 条动态正则模式（赞/评论/回复带昵称、能量退款带格数单复数、投票审核带标题与原因、解除关系带昵称、情侣 100 次我爱你），标题/正文/时间戳标 data-no-i18n 与全局词典观察器隔离；界面词补词典键（Notifications=通知/Notification=通知详情/No notifications/You're all caught up/Failed to load/Loading...）。②**formatPostTime 语言感知**：中文态输出 刚刚/N 分钟前/N 小时前/N 天前/zh-CN 日期（全站帖子/评论/通知时间戳统一中文化）。③**通知列表+详情两处头部**：标题与返回键从左侧挪到右侧（右滑面板关闭方向为右，arrow_forward 紧贴右缘）。浏览器 mock API 实测：英文态头部布局（标题在前/按钮贴右/箭头向右）+5 类通知原文；中文态列表与详情标题正文全译（含「沐晨 赞了你的帖子/退还 3 格能量/投票「食堂哪家强」未通过审核」）、分组 今天/昨天/更早、时间 5 分钟前/3 小时前/1 天前、空态 暂无通知/都看完啦；vite build 过。未提交（等推送指令）。

### 2026-07-26
- 完成（评论区重排 + 间距复核）：①帖子详情评论区——楼层分组（父楼+回复包成组，组间 28px/组内 16px，替代原一律 40px 平铺），回复行头像缩小 w-7 + pl-11 缩进对齐父楼内容，评论正文改主色更醒目，Reply 键收紧；评论输入条从下划线改软填充胶囊 + 荧光绿圆形发送键（对齐全站软填充输入风格），空态加 forum 图标居中卡；Observations 标题拆「可译词+计数」并新增 i18n 键=评论。②帖间距复核：小卡+跨栏校园墙卡+广告卡混排实测垂直间隙全部恒等 6px（上一条瀑布流修真已覆盖，卡片自身无额外 margin）。浏览器实测分组/头像/AUTHOR 徽标/软填充样式/空态/回复条显隐/中文态「评论 (N)/回复」全过；vite build 过。**本轮三项已提交上线（b89ab4c）**：推 unipia+server、重建 h5 容器，线上 bundle 哈希与本地构建一致、CSS row-gap:0 生效、API 重启后 site-stats 正常。
- 完成（发帖弹窗重排 + 推荐页去投票）：①投票行整块仅校园墙发帖时显示（推荐页加号里不再出现，openNewPost 按 newPostBoard 切 #newpost-poll-row）；②布局重排——去向提示改圆角 chip、写作区（标题/正文/图片）提到最前、匿名+投票开关收进底部圆角卡片组（分隔线行式），投票选项输入改白底与卡片区分；③i18n 新增 'Posting to'=发布到、投票提示键改 'Goes live after review'=审核通过后展示。浏览器实测：推荐路径投票行隐藏、DOM 顺序正确、开投票后选项进卡片+去向联动 Campus Wall、中文态三处文案全译；vite build 过。未提交（攒批）。
- 完成（广场瀑布流垂直间距修真）：帖子上下间距此前虽名义 6px，但 2px auto-row + 6px row-gap 经 span 向上取整后实际浮动在 6–13px；改为 1px auto-row + row-gap:0，6px 间距由 layoutSquareMasonry 精确算进 span（span=⌈h⌉+6）。浏览器注入 6 张异高模拟卡实测同列间隙全部恒等 6px，vite build 过。未提交（等本轮 H5 改动攒批）。
- 完成（广场收尾 + 启动自愈 + 多代理审计修复，均已上线）：①广场帖子上下间距统一 6px、卡片圆角改小、发帖去向随所在页（不再切换推荐/校园墙，97962d0）；②修 e4abf79——上一轮删「偏好模式切换」时误删了滚动容器与恋人区标题，偏好面板结构破损/不能滚动，按行补回；③**启动看门狗**（657192f）：手机主屏图标常卡在启动页，根因是旧缓存的 index.html 引用已被删除的 hash 资源→404→模块永不执行；`<head>` 内置监听资源加载失败 + 7s 未 boot 则带时间戳重载一次（sessionStorage 防循环），二次失败显示可点重试的降级页；同时 nginx 对 index.html/manifest 置 no-cache、/assets 一年不可变。
- 完成（多代理审计 05622a8..97962d0，22 项确认 / 13 项驳回 → 去重后 14 项全部修复并验证，e9e885b）：**手势**——滑动返回手势中途多一根手指/touchcancel/重入会把弹层永久留在「已位移+半透明+touch-action:none」（HIGH），改无条件 resetPanel；广场横滑 settle 因触点卡片被 innerHTML 替换而收不到 touchend，轨道永久停在两页各露一半（HIGH），改绑 document + 非水平手势也复位。**匹配**——「已在池中」后端早返回不扣费，前端却报成功/亮增强徽标/复位开关，买增强成空操作（HIGH）；确认卡点背景返回 null=中止（与显式「不用增强」区分）；增强开关 await 期间保持受控并禁用；匹配设置回填加竞态令牌+模式校验+输入框标脏，偏好加载失败时保存跳过 extraMatchInfo（此前空白会覆盖服务器已有文本）。**广场**——收起搜索条不再清空关键词与结果（点结果进详情返回后结果还在），有筛选时右上角图标显荧光绿，点信息流不收起；搜索条撑高顶栏时同步内容区上边距；取消投票还原原发帖去向；加号在广场页隐藏时 resize 不再被甩到左上角；点赞同步两页同帖副本；校园墙未绑学校提前引导补资料，避免图片白传。**i18n**——移除过短通用词典键（Title/Note/Date/YOU/Today），聊天气泡与拍一拍内容加 data-no-i18n，用户内容不再被误翻。验证：五模块 node --check、vite build、自建 healthcheck（内联处理函数/标签平衡/重复 id/CSS-JS 常量一致/手势清理路径）全过，线上 bundle 逐字符串核对 6/6 命中。
- 待办：本机 Docker 仍未恢复（开发库 titleEn 回填待补，生产已完成）；广场搜索后端语义（是否上搜索引擎）待定；matching-ml 接真实 LLM；P0-3 公布 cron 运营切换。

### 2026-07-24
- 完成（问卷双语 + 题目导航，已上线 9d1cea5 并生产验证）：①**题库双语**——Question 加 titleEn 列，75 题（恋人50/朋友25）全部由子代理翻译成口语化英文并写入 seed；新增幂等回填脚本 prisma/backfill-question-en.ts（按中文题面 updateMany，编入 dist）；生产容器内跑回填 75/75 全更新。②**H5**——英文态显示 titleEn（缺省回退中文）、开场提示双语、量表五档/问卷页/卡片残留补中文键；新增**题目导航弹层**（网格跳任意题：绿=已答/描边=未答/圈=当前）。③线上 E2E：英文态 50 题全英文、跳第 30 题正确；中文态原题+「非常同意」+答题后导航绿块标记，全部通过。④界面批量优化七项（3671d86）：匹配中只留动画+倒计时（动画 240px）、Any age 勾选荧光绿、偏好弹层收紧+下拉关闭、修增强开关被 preferences 回填重置的「保存不了」bug、设置排版、编辑资料重排（头像封面对齐相机角标/全输入去下划线改软填充/礼物罐精简）、认证与聊天搜索去下划线。
- 排障（本机 Docker Desktop 4.78 起不来）：根因 = Inference 服务的残损 unix socket（AppData\Local\Docker\run\dockerInference 系统级不可删）导致启动即崩溃；run 目录整体改名重建后仍触发用户点了 Reset to factory defaults，重置后首启轮询中。开发库 titleEn 回填待 Docker 恢复后补跑（生产不受影响）。

### 2026-07-21
- 完成（遗留功能收尾，已上线 a5d208d）：①**P1-6 行为埋点**——H5 接入 viewed/openedProfile 上报（POST /matching/feedback/events）：恋人匹配卡/朋友候选卡渲染报 viewed，聊天头像打开对方资料报 openedProfile；会话内 Set 去重、失败允许重试，实测请求体正确且去重生效。②**内容页中文版**——帮助中心/安全提示/用户协议/隐私政策四页完整中文翻译（CONTENT_PAGES_ZH），中文态整页切换、英文态原文回退，实测双语正确。③**生产库核验**——square_poll_votes/events/event_tickets 三表与 square_posts 新列（postType/reviewStatus/eventId）确认已在生产库建好（投票/活动功能的唯一约束随新表创建无需 --accept-data-loss）。
- 待办：matching-ml 接真实 LLM（LLM_BACKEND=mock，需 API key）；官网 hero 视频改版待定；P0-3 公布 cron 运营切换；帮助中心内容后续可从 FAQ 数据源统一维护。

### 2026-07-18
- 完成（投票+活动两大功能 & 九项界面改造，全栈落地并 E2E 全绿）：①**校园墙投票（审核制）**——SquarePost 加 postType/pollOptions/reviewStatus + SquarePollVote 表；人人可发起（强制校园墙、2-6 选项），创建即 pending 仅作者可见，学生会审本校/团队审全量（/admin/square/polls，团队视图带 hasUnionReviewer 徽标），审核结果通知作者；H5 发帖开关+选项输入、卡片占比条投票/改票；admin-web /moderation 加投票审核 tab。②**活动+门票**——新 events 模块（Event/EventTicket），学生会/团队发布活动同时生成广场活动帖（feed 直出时间/地点/票价），购票 mock 支付、容量条件自增防超卖、票码核销防重复；H5 活动卡+详情购票+「我的票夹」（票卡+二维码 qrcodejs）；admin-web 新 /events 页（发布/停售/取消/购票名单/入场核销）。③注册必填信息（昵称/姓名/性别/生日）改分步向导每项一屏（空值拦截+进度条）。④底部导航改悬浮胶囊+滚动下滑隐藏上滑恢复；⑤启动页重设计（平面 logo+进度线，去圆环点阵）；⑥四个侧滑面板改 86% 宽留边+圆角；⑦Chat 左上角改加号→小弹出卡（搜索/扫码/关系网/深色/语言，直达面板）；⑧match 顶栏去阴影、40px 同轴对齐；⑨profile 移除 Logout。验证：API tsc+jest 20/20、H5 vite build、admin-web next build 全过；后端 E2E 12 步全绿（pending 拦投票/作者自见/审核/投票改票/通知/活动 feed/超卖守卫/票夹/核销防重复/名单）；浏览器实测向导四步、投票 UI、购票到票夹二维码、底导隐藏、加号菜单、顶栏对齐。注意：**Dockerfile 的 prisma db push 无 --accept-data-loss，本次新列唯一约束需在服务器手动跑一次 db push --accept-data-loss 再起容器**。
- 完成（H5 三项跟进：通知图标扁平化 / Chat 三点图标 / 下拉刷新，已上线）：①通知列表与详情的图标盘去描边改浅灰圆角方块（.notif-icon-plate，深色模式适配），通知空态/加载失败态统一 flatEmptyIcon；②match 界面 Chat 视图左上角 forum→more_horiz 三个点；③新增 attachPullToRefresh 通用下拉刷新组件（core.js）——白色圆片从顶栏下滑出、跟手下降+按进度旋转、过阈值变绿松手转圈刷新、最少展示 600ms 后收回；绑定 match 页（chat 视图刷会话/匹配视图刷状态）与 square 页（刷当前信息流）。验证：vite build 过、浏览器合成 TouchEvent 实测下拉全生命周期（跟手 110px/ready 态/转圈/触发一次刷新/收回）、真实 like 通知图标盘渲染正确、无控制台报错；已提交推送（f53e449）并重建线上 h5 容器，线上 bundle 已含新代码。
- 完成（H5 应用端 11 项体验修整，全部实测通过）：①注册资料页年龄→**生日**（date 输入限 16–40 岁区间，age 前端按生日推算随生日一并提交；后端 Profile 新增 birthday 列 + DTO 校验，db push 完成，实测 2004-03-15→age 22 落库）；②必填星号补齐（昵称/真实姓名/性别/生日四项）；③注册页加兴趣补显式 Add 按钮（手机软键盘无回车也能加）；④设置页新增 Preferences 区（语言切换+深色模式入口），i18n 中文字典大扩容（设置整页/匹配空态/广场/能量购买/注册资料全覆盖，中文态实测全译）；⑤广场推荐流改**小红书式瀑布流**——图片卡高度随图片原始比例（min110/max300 收敛），纯文字卡固定 3:4；grid-auto-rows:2px + JS 按卡高设 row-span，修了一个图片加载完成早于监听挂载导致不重排的竞态；⑥空态平面化重做（新 flatEmptyIcon helper：圆角方块图标，去圆环）——问卷引导/无聊天/无匹配/无朋友/资料不可用/广场空态/加载失败七处统一；⑦match 顶部三段切换：黑边 1px→rgba(0,0,0,.08) 细浅边+轻投影，按钮按文字内容分宽不再三等分挤压；⑧能量购买流程重做：选套餐→选支付方式（单选行）→「Pay ¥X · N cells」统一确认（下单推迟到确认时，不再产生半截订单；实测 60 格到账）；⑨全站邮箱 support@campuslove.app→contact@unimatcha.ai（设置内容页 5 处+联系我们弹窗 2 处）；⑩中英混杂清理：闪屏旧品牌「Campus Love」→Unimatcha、拍一拍设置区英文化（后端 nudge 消息本就是英文）、版本行去「抹茶/Oxford Campus」；⑪顺手修 bug：设置页删假数据行（Phone/+1 555、Blocked 12 accounts、Visibility）、删设置页会误存匹配偏好的 Save Preferences 按钮（各开关本就即时保存）、登录页 Terms/Privacy 死链接改为打开对应内容页、©2024→2026。验证：API tsc+jest 20/20、H5 vite build 过、浏览器实测注册→资料→匹配/聊天空态→广场瀑布流（6 卡 span 与实高全对齐）→设置中英双态→能量购买闭环，控制台无报错。测试数据 bday.test@campus.edu + 5 条测试帖留在开发库。
- 完成（第二屏右栏对齐与间距定稿）：栅格改确定性 340px+minmax(0,1fr)/gap 66px（fr+min-content 撑爆问题根除），folder/topics 去宽度上限填满列位——右栏右缘与上一屏步骤列右缘像素级对齐（1225=1225，同 wrap 内容盒），手机-内容间隙 66px=原 44 的 1.5 倍。排障备忘：测量时 ±72px 的诡异偏移是 .reveal.l/.r 未触发的入场初始位移，量布局前先给 reveal 加 .in（919ac58 已上线）。
- 完成（交叉淡化收尾两修）：①手机底部阴影被截出硬边——根因是 #features/.showcase/.showcase3 自带 overflow:hidden，紧凑间距下阴影出界被裁；#showpin .show-stage 放开 overflow:visible。②第二屏手机贴列左、栅格改 .74fr/1.26fr（右栏 ~716px），wall-folder 拓 620px/lead 56ch，右侧内容纵向不再拥挤；≤860px 单列恢复居中（adbdc05 已上线）。
- 完成（钉住滚动改连续交叉淡化）：用户反馈钉住太硬卡——移除 sticky 劫持，改正常文档流：滚动时按每屏中心距视口中心的距离实时算透明度+轻位移（±0.25vh 满显、±0.85vh 淡完，rAF 节流），几何上第一屏离开满显区开始淡出时第二屏恰好进入淡入区，丝滑交叉、滚轮零劫持；共享背景/紧凑间距保留，≤920px 恒显回退（652c1b8 已上线）。注：本机冻结渲染环境连 scrollTo 都会被吞，滚动类交互只能解析验证+真机确认。
- 完成（两屏手机改钉住滚动）：#features/#wall 包进 #showpin（240vh 行程 + sticky 100vh 视口）——背景（米白+点阵）固定大小不动，滚轮驱动两屏内容淡入淡出互斥切换（p<0.5 第一屏 / ≥0.5 第二屏，绝不同屏），两屏距离由此归零；#wall 锚点映射到行程 80% 处；≤920px 回退正常文档流；矮视口档位缩手机/收纳次要内容防溢出。排障两枚：①bash 单引号包 node -e 时把 querySelectorAll 选择器的引号吃掉 → #wall 被解析成类私有字段、整段脚本语法死亡（轮播全停），改用文件读写替换修复；②本机冻结渲染环境下程序化 scrollTo 不派发 scroll 事件，手动 dispatchEvent 验证互斥逻辑双向正确（9f88348 已上线）。
- 完成（两屏手机展示统一浅色底）：用户改口不要暗区——#wall 改为与 #features 完全同底（米白 var(--ivory) + 24px 点阵网格），旁侧内容整体转浅色：h2 墨色+em 荧光绿高亮划线、lead #555、wall-tab/panel 白底 var(--line) 细边、chips 白底、explore/label 用 muted、手机阴影去霓虹光晕与第一屏一致（4f51a4c 已上线）。
- 完成（校园墙暗区视觉调整）：#wall 背景纯黑 #101311→黑灰 #232624，点阵纹理改 44px 线网格（顶部聚焦渐隐 mask）；删除手机背后的 rings canvas 圆环动画。已上线（ab1a737）。
- 完成（上线后官网三修）：①首访默认语言 zh→en（index/site.js，index 标题随语言切换补齐）；②手机演示翻页从 scroll-snap 重写为 transform 轮播（拖拽/触屏跟手、18% 阈值翻页或回弹、touch-action:pan-y 不挡竖向滚动、悬停暂停自动翻页）——原生滚动+snap 在真实浏览器里与拖拽/自动翻页互相打架；③全站邮箱 hello@→contact@unimatcha.ai（含 chat.js 知识库 11 处）。本地+线上双验证：新 origin 首访英文/英文标题、模拟拖拽翻页、线上活数据 ROUND 001（CORS 白名单工作正常）；website 容器已重建上线（952163c）。

### 2026-07-17
- 完成（生产部署到 DigitalOcean，unimatcha.ai）：①域名确认为 **unimatcha.ai**（Spaceship），全局替换 .com→.ai 约 20 处（官网 API 地址/子域链接/邮箱/OG/sitemap/Caddyfile/DEPLOY/admin 占位符）并推 unipia；②七轮改动整理为 6 个提交推送（api 公开接口/admin 联动/官网 v2/部署配置/日志/域名切换）；③新建 DEPLOY.md 部署手册（DNS 5 条 A 记录、.env 模板、验证清单、常见问题）；compose 补 ALLOWED_ORIGINS 接线；④服务器 root@209.97.179.143（DO 伦敦 2vCPU/4GB，known_hosts 里找到的既有机器，本机 ed25519 免密直连）：unipia 私仓服务器拉不了 → 改为**本地 git push server main 直推服务器裸仓库** /opt/unimatcha.git + post-receive 自动检出 /opt/unimatcha；生产 .env 服务器上随机生成（密钥/管理员密码/MATCH_API_KEY，SEED_DEMO=false）；⑤修一个真构建 bug：website 镜像 nginx.conf 被 .dockerignore 排除导致 COPY 失败（b0df25a：保留上下文、构建后从网页根删除）；⑥八容器全 Up：api 日志 prisma push+seed+Nest started，容器网内 site-stats 返回生产真实数据（全 0、下轮 2026-07-24 五 17:00 沪时），website 出 HTML，postgres/redis healthy。**唯一待办：用户在 Spaceship 加 5 条 A 记录 → 209.97.179.143**（@/www/app/admin/api）；Caddy 已在自动重试签证书（最长重试 30 天），DNS 生效即上线。
- 完成（🚀 正式上线，全站验证通过）：DNS 走了一段弯路——用户先在 Spaceship 面板加记录但域名 NS 还挂在 launch1/2.spaceship.net 停放服务器（TLD 还有旧 DS 记录导致公共 DNS SERVFAIL），诊断后用户切 NS + 关 DNSSEC；最终 **NS 托管在 DigitalOcean（ns1-3.digitalocean.com）而非 Spaceship DNS**，记录在 DO 控制面板管理（主域自动建，www/app/admin/api 四条用户手动加）。验证清单全绿：五域名解析 →209.97.179.143、五 HTTPS 端点全 200（Caddy 证书自动签发，重启一次触发即时补签）、site-stats 生产真实数据、官网静态资源 200、**公网合作表单提交→public_submissions 落库（PENDING，后台工作流就绪）**、admin@unimatcha.ai SUPER 登录 OK（凭据仅在服务器端测试）、八容器 Up。测试数据 launch.test@unimatcha.ai 留在库中可在后台「官网提交」页查看。
- 待办 / 明日计划：admin 后台浏览器端人工过一遍；H5 注册真实用户冒烟；正式公布 cron 切换（P0-3，scripts/set-weekly-schedule.sh）；matching-ml 接真实 LLM（现 LLM_BACKEND=mock，规则打分正常）；SEED_ADMIN_PASSWORD 建议用户登录后修改。

### 2026-07-16
- 完成（全站语言纯净化：中文态全中文 / 英文态全英文）：用户要求逐页检查，包括手机演示和输入框。①index applyLang 补通用 [data-zh]/[data-en] 处理（与子页 site.js 同机制）；②两台手机 mockup 全部文字双语化——顶部聊天/恋人/朋友分段、底部匹配/广场/我的三签（×6）、匹配进行中/距下轮公布/2天 09:49:30/离开匹配池/修改匹配偏好、问卷页、聊天空态、推荐/校园墙 tab、搜索帖子、三条帖子内容与作者（沐晨↔Morgan、青柠↔Noble Hare、失物招领↔Lost & found…）、Profile 页（认证/未在匹配中/能量/编辑资料/联系我们/设置）；③faq 搜索框占位符分语言（data-zh-ph/en-ph）；④safety 视觉卡人名/兴趣标签（音乐/摄影/羽毛球）双语、红线跑马灯六项补双语；⑤中文态各页 hero eyebrow 去英文后缀（安全与信任 · SAFETY→安全与信任 等 6 处）、index epo.label 同理。**iframe 审计脚本**逐页扫描文本节点：英文态 7 页 CJK 残留=0（仅语言按钮"中"为功能标识），中文态残留仅剩图标 ligature 名与专有名词；手机 mockup 中英实拍/JS 逐项核对全部正确翻转。
- 完成（品牌标 Beta 化）：全站 8 页（index+4 内容页+2 法务页+404）页眉页脚品牌标去掉左侧霓虹圆点（.blip），改为 UNIMATCHA + 右上角荧光绿 BETA 小标（.beta-tag，mono 9px 上标式），官网整体包装为 beta 版本；site.css 与 index 内联样式各加一份，JS 校验 8 页全生效、无 blip 残留。
- 完成（赞助页定位重做：学生会合作 + 全双语演示）：用户澄清赞助页定位——不是「给钱办活动的申请」，而是**找学生会合作**：表单改为「合作联系表」（学生会名称/邮箱/想聊什么，仍走 sponsor-application 接口），介绍主打**免费定制学生会管理系统**并突出展示（暗色区三视图演示：学生会总览/校园墙管理/**新增广告审核**——待审黄标+通过/驳回，紧贴真实 ads 审核流），赞助保留强调但不展开细节（霓虹满幅 band「我们还真金白银地赞助你的活动…具体聊了才知道」）；hero 改「我们在找学生会合作。」+PARTNER 描边字。**演示全双语**：三个后台 mockup 内部所有文字（侧栏/标题/表头/单元格/胶囊/按钮/示例帖子与广告内容）全部 data-zh/data-en 化，英文态整个演示是全英文（Union Overview/Weekend café study buddy/Freshers bubble-tea BOGO…），中文态全中文，实测切换逐项核对。页面 title 改「学生会合作 · For Student Unions」；#apply 锚点保留（index CTA 与 chat.js 链接不破）。实测：三 tab 切换、双语整体翻转、联系表单提交成功态正常。
- 完成（官网赞助申请 ↔ 团队后台联动，spec §5.6）：①模型——PublicSubmission 补处理工作流（status PENDING/CONTACTED/APPROVED/REJECTED、handledByAdminId/handledAt/handleNote、convertedAdminId、updatedAt + [type,status,createdAt] 索引，db push 完成）；②后端（admin 模块，SUPER/TEAM）——GET /admin/submissions（type/status/search 筛选 + convertedAdmin join）、PATCH :id（状态流转，APPROVED 仅可经开通达成）、POST :id/convert（一键开通：学生会=选校或事务内新建 School，商家=组织名+可选来源学校；邮箱冲突 400；事务内建号 bcrypt12 + 置 APPROVED + 回传一次性密码）、团队 dashboard 增 pendingSubmissions；③前端——/submissions 页（赞助申请/候补名单双 tab、状态筛选+去抖搜索、留言查看、标记已联系/关闭/重新打开、开通账号 Modal 含随机密码生成与一次性凭据卡）、侧边栏「官网提交」、总览第三个待办 chip。E2E 实测七步全绿：官网表单提交→后台 PENDING→已联系→开通（新建 University of Leeds + 学生会账号）→新账号登录角色/绑校正确→提交记录 APPROVED+重复开通 400→角标计数正确。与同日赞助页「学生会合作」定位正好闭环（合作联系表 → 后台开通学生会账号）。注意：bash curl 在 Windows 控制台发中文会写入乱码（API/真实表单无此问题），本轮测试数据已用 Prisma 脚本清洗，后续测试中文一律走 node/PowerShell 发请求。
- 待办 / 明日计划：官网+api 多轮改动仍未提交（含本轮联动）；admin 收到新提交的站内/邮件提醒可作后续增强。
- 完成（赞助页重排 + FAQ 搜索修真 + 再扩容）：①sponsors.html 按用户要求重新设计：删除跑马灯，改为「介绍 + 填表」为主——左栏编号介绍（我们出什么 4 项 + 纵向合作流程 4 步 + 承诺点列）、右栏**粘性跟随的申请表卡**（.sp-main/.sp-form-wrap），后台双视图展示区下移做支撑；顺手修了一个真 bug：.form-ok{display:flex} 覆盖 hidden 属性导致「申请已收到」成功卡常驻显示，补 .form-ok[hidden]{display:none}。②FAQ 搜索改为真搜索：itemHaystack 同时索引 data-zh/data-en 原始文案（惰性缓存 __hay），命中与当前显示语言无关（中文态搜 anonymous / sponsor 均命中，sponsor 跨 3 分类 5 条）。③FAQ 32→45 问（8/11/8/7/6/5）：多账号/收不到邮件、双模式并行/错过公布/解除后再匹配、字数限制(标题100/正文2000)/SPONSORED 标注、朋友格数怎么选/对方不知增强、识别假官方/防机器人、赞助无门槛/被拒可再申。④index FAQ 入口文案改为不带数字（前往帮助中心查看全部问题），避免每次扩容改数。实测：赞助表单新版提交落库（测试社团B）、成功卡切换正常、45 问筛选/搜索/清空全通过。
- 完成（官网手机 mockup 重做：按真实 H5 浅色界面 1:1 复刻 + 多页滑动）：用户指出 7/14 版 mockup 不是真实 H5 界面（且 _clip-a/b.mp4 是留给 hero 改版的素材，已恢复原名不接入）。本轮实际跑起 H5（docker start unimatcha_postgres/redis + launch.json api/h5），注册演示号 site.demo@campus.edu（资料沐晨/UCL、问卷 50 题、入恋人匹配池、播种 4 条广场帖），逐屏截图核对真实浅色 UI 后重写两台手机为可滑动 3 页轮播（共 6 个真实页面）：匹配机（恋人匹配中·线稿小人+倒计时+粉色 Leave Pool / 问卷引导 / Chat 空态）、广场机（Campus Wall 白卡信息流·匿名化名 Noble Hare / Recommend 瀑布流文字卡 / Profile·Verify+能量+菜单行）。设计令牌取自 H5 真源（bg #f9f9f9、顶部 Chat/Romantic/Friend 荧光绿胶囊分段、底部 Match(∞ SVG)/Square/Profile 三签、10px 圆角、宽字距标签；粉色仅出现在应用画面内部，与真实一致）。轮播：scroll-snap + 自动 3.8s 翻页 + 桌面拖拽 + 圆点导航；修复 aui-track overflow-y 引起的纵向偏移、顶部内容避开豁口。验证：Chrome 实测 6 页逐页截图核对（本机 Chrome 渲染节流冻结 CSS 过渡导致截图需禁 transition，系环境问题非代码问题）。临时 iframe 取景文件 apps/h5/public/_frame.html 已删。
- 完成（页眉改版 + 部署对齐，用户拍板「全子域名式」）：①页眉去掉整排站内锚点（功能/校园墙/评价/合作伙伴/FAQ），改为 品牌 | 进入应用↗（https://app.unimatcha.com，新 .applink 样式+外链箭头）| 语言切换 | 荧光绿 CTA「获取 App」→#getapp；admin.unimatcha.com（学生会/商家登录）按用户要求官网全站不显示；页脚「H5 移动端」改指 app 子域。②i18n 补 nav.open，nav.cta 改「获取 App/Get the App」。③响应式：≤920 原来直接藏导航（移动端无导航的老问题随锚点移除自然消解），新增 ≤420 收紧规则，375px 实测一行放下（修了「进入应用」竖排换行 + 一次插错 560 媒体块位置的结构问题）。④部署对齐：新建 apps/website/Dockerfile（nginx 静态托管），compose 的 web 服务（原指旧版 apps/web Next 官网——上线会跑错站的隐患）替换为 website 服务，Caddyfile 主域名 reverse_proxy 改 website:80，caddy depends_on 同步，compose config 校验过。注意：app.unimatcha.com 未部署前页眉链接会 404，属预期。
- 完成（官网新增四个内容页 + 页眉定稿）：用户澄清页眉要挂**新写的内容页**（此前「全子域名式」理解有偏差），且「首页有的都不要」（匹配/校园墙不单独成页）、后台能力要展示但**不放 admin 登录链接**、右侧简化（去掉进入应用↗）。落地：①共享脚手架 site.css/site.js（同源设计令牌、页眉页脚组件、data-zh/data-en 属性 i18n + data-lang 内容块、um_lang 与 index 互通、≤920px 汉堡抽屉、当前页高亮、reveal/FAQ 手风琴/console 标签页）。②四页全双语：sponsors.html（赞助计划——四项投入卡、**照真实后台复刻的浏览器框 mockup 双视图**「学生会总览/校园墙管理」、四步合作流程、mailto CTA）、safety.html（.edu 认证/化名机制/举报三步/数据四底线/社区红线）、faq.html（5 分类 16 问手风琴 + 前端搜索过滤）、about.html（宣言/三信条/由学生打造/时间线）。③index 页眉换成四页导航 + 汉堡（移动端首次有导航），页脚「关于」列补四页链接；nav.cta 维持「获取 App」。素材核对：为看真实学生会后台，dev 库建 sch_demo_ucl 学校 + union.demo@campuslove.com（STUDENT_UNION，密码同 SUPER seed），实登 admin-web 截图 /dashboard 与 /moderation 逐屏对照复刻。验证：四页桌面截图、赞助页双 tab 切换、FAQ 搜索（"能量"过滤 4 条）与手风琴、中英切换（title/导航/内容块联动）、375px 汉堡菜单展开与无横向滚动、index 导航/页脚链接 JS 校验全通过。
- 完成（四页设计感重做 + 官网接真后端）：用户反馈「页面太丑不要只是卡片列出来」+「网站要对应后端」。**后端**：新增 public 模块（apps/api/src/public，无鉴权）——GET /public/site-stats（真实 users/matches/schools/roundsCompleted/currentRound + 用 MatchConfig.cronExpr 经 CronJob.nextDate() 算 nextRevealAt，60s 内存缓存）、POST /public/waitlist、POST /public/sponsor-application（新 Prisma 模型 PublicSubmission，(type,email) 唯一幂等 upsert，不泄露是否已存在）；db push + tsc + jest 20/20 全过；旧 3001 进程是 7/13 的 dist 产物，已重启为 start:dev。**前端接线**：index 统计数字（data-stat 标记，7 处）、ROUND 标签、倒计时（CD_TARGET 用后端 nextRevealAt，失败回退本地周五 17:00）、候补名单 mailto 改真表单（POST 失败回退 mailto）；about 统计带 data-live-stat；sponsors 申请表单（组织/邮箱/留言 → sponsor-application，成功态卡片，失败回退 mailto）；API base 按 hostname 自动切 localhost:3001 / api.unimatcha.com。**设计感重做**（site.css 新组件：斜切跑马灯 .mq、编辑部式编号大行 .edrows、分栏 .split、暗色区 .sec-dark、大描边字 .bigword、视觉小卡 .vis、表单 .form-card、FAQ 粘性侧目录 .faq-layout）：sponsors=描边 SPONSOR+霓虹斜跑马灯+编号大行+暗色后台展示区+分栏申请表单；safety=描边 SAFE+认证资料卡/匿名化名转换两个视觉卡分栏+暗色四底线编号行+红线黑跑马灯；faq=描边 FAQ+左粘性分类目录（滚动联动高亮）；about=描边 MATCHA+宣言下接真后端的统计带+信条编号行。验证：三接口 curl 通、两表单实测落库（psql 查到 WAITLIST/SPONSOR 行、中文无乱码）、首页 ROUND 004/真实统计/后端倒计时、375px 溢出修复（bigword 移动端隐藏+html overflow-x hidden）、FAQ 双栏。
- 完成（补全收尾）：①新建 terms.html 用户协议（双语 9 节：资格 18+/.edu、行为红线、内容授权、能量非货币不可提现/按轮退还、免责、账号处置），privacy.html 重构到共享脚手架（统一页眉页脚导航，隐私条款补「官网提交」类别并互链 terms），全站页脚（index+4 子页+两法务页）补用户协议链接；②SEO/部署基建：robots.txt、sitemap.xml（7 页）、404.html（"这一页没匹配上"）、nginx.conf（error_page 404 + 分类缓存策略）接入 website Dockerfile（.dockerignore 排除）；③四子页补 OG 标签；④API 加固：public POST 端点加进程内 IP 限流 guard（60s/10 次，实测第 11 个 429），CORS 改 ALLOWED_ORIGINS 环境白名单（未设保持放开不破坏 dev/H5）；tsc 过、jest 后台跑。admin「官网提交」查看页已由用户在独立会话启动（task_6c64c1f7）。
- 完成（FAQ 扩容 + 全站智能客服 + 页面联动）：①faq.html 16→32 问，新增「赞助与合作」分类（学生会申请/商家投广告/后台能力，链接 sponsors），各分类补齐（注销/资料可见性/确认后流程/不确认会怎样/同校偏好/带图发帖/化名不可拼接/下架申诉/能量获取/增强不保证/线下安全/官网邮箱用途），左侧目录计数更新 6/8/6/5/4/3；②新建 chat.js 自包含悬浮客服组件（自注样式+DOM，7 页一行引入含法务页）：荧光绿悬浮球+聊天面板、16 个意图的中英知识库关键词评分匹配、打字动画、快捷问题 chips、答案可带跳转链接、fallback 引导帮助中心+人工邮箱、语言跟随 um_lang（MutationObserver 监听 html lang）；实测中文命中赞助/能量意图、fallback 兜底、chips 正常；③联动优化：index 赞助 CTA 从 mailto 改指 sponsors.html#apply 表单、index FAQ 区块尾加「查看全部 32 个问题 →」（i18n faq.more）。④FAQ 左侧分类按用户要求从「滚动定位」改为「筛选式」：点哪类只显示哪类（默认第一类），支持 #g-xxx 深链直达（chat.js 的能量链接正好用上）、切换写回 URL hash；搜索时跨全部分类过滤、清空恢复当前分类（搜索匹配当前显示语言）。实测：初始仅账号类 6 问、点能量只剩 5 问、中英搜索均命中、清空恢复。
- 待办 / 明日计划：hero 改版是否用 _clip-a/b.mp4 待用户定；about 团队故事等真实素材；生产部署时设 ALLOWED_ORIGINS；chat.js 知识库后续可改为从 faq.html 数据源生成避免两处维护；官网+api 七轮改动未提交。

### 2026-07-14
- 完成（官网补全为「完整可上线」）：①两个手机 mockup 从虚线占位改为手写应用内界面（恋人匹配揭晓页 + 广场/校园墙信息流，`.aui-*` 样式、词汇取自 H5 真实 UI：聊天/恋人/朋友、推荐/校园墙、确认为恋人、增强匹配能量格；保留 `.ph-media` 截图/录屏替换约定注释）——注意 `_clip-a/b.mp4` 是留给 hero 改版的备用素材（卡片墙配对 / 两人相遇，1280×720×10s），不接入手机屏、不删除；②赞助按钮死锚点 `#get-sponsored` 改为带主题的 mailto；③页脚死链接清理（H5/iOS→#getapp，删管理后台外链，隐私协议→新建 privacy.html）；④新建同风格双语 privacy.html（8 节隐私协议，共享 um_lang，中英内容块按 html[lang] 切换）；⑤`<head>` 补 favicon（SVG data-URI 霓虹点）、OG/Twitter 分享卡、theme-color，标题/描述同步「匹配 × 校园墙」双定位；⑥App Store/Google Play「敬请期待」徽章去掉 `href="#"`（点击不再跳页顶）；⑦移动端 ≤560px 原 `.screen{height:280px}`（占位时代规则）改为整体缩小手机宽 272px 保持真机比例。预览验证：桌面+移动双视口截图、两 mockup 渲染正确、隐私页中英切换、全站 0 死链接、控制台无报错。
- 待办 / 明日计划：hero 改版是否用 _clip-a/b.mp4 待用户定；候补名单目前为 mailto，可考虑接表单服务；服务器部署 matching-ml + 配 AI_PROVIDER_URL/MATCH_API_KEY（P0-1 部署半）；正式切周日公布 cron（P0-3）。

### 2026-07-13
- 完成（iOS 端补齐重写 + 后端核对）：后端经全量契约测绘核对为「对所有客户端功能完整」（唯一缺口 SMTP 发码为外部集成、头像走 /uploads/image、拉黑无表均为有意留桩），无需补接口。iOS 端从「单模式+粉色+已下线排行榜」的旧世代整体重写到与当前后端对齐：①基座——App/Theme.swift 霓虹绿暗色设计令牌（消灭各文件内联粉色字面量）、APIClient 加 multipart 上传、Models 按域拆分（Auth/Profile/Matching 双模式/Chat/Square v2/Couple/Energy/Notification/Questionnaire/Common）；②Network——12 个 Service 按真实契约重写（MatchingService 双模式 status/result/start(enhanced,cells)/connect/confirm(matchId)/dissolve、ChatService sessions/nudge/background、SquareService v2 recommend/campus-wall/detail/like/comment/report、CoupleService/EnergyService/NotificationService/UploadService/ReportService 新增），删 LeaderboardService；③ViewModels 全域（Matching 双模式、ChatSessions、Couple、Energy、Notification、Square v2 分页、Questionnaire 按 mode）；④Views 24 屏（匹配双模式+增强能量 sheet、聊天列表+会话+拍一拍、广场 v2 信息流+详情+发帖、情侣空间、能量充值、通知、我的+编辑+设置+连接码 QR、鉴权+引导+问卷），删旧粉色 MainMatchingView/ChatView/RelationshipModeView/重复 MainTabView/EnergyView；⑤可构建——XcodeGen project.yml + Info.plist（API_BASE_URL/ATS）+ Assets（AccentColor 霓虹绿）+ README，取代原「手动建工程」的 README_SETUP。验证：本机无 Swift 工具链（Windows），用并行 opus agent 逐文件对锁定的 Models/VM/Service 静态复核（相当于编译器），24 屏仅 1 处调用点不符已修（ProfileTabView 的 MatchFilterView），全域无类型名冲突、无遗留粉色、无失效符号引用。共 60 个 .swift。
- 完成（找 bug 第三轮：处理上一轮留给用户决定的 6 项，opus 深挖 fix-plan）：用户给了退款语义（朋友：消耗>匹配朋友数则退多余；恋人：没匹配到退全部）+ 确认 friend 模式为轮次制。据此：①【修·退款】expireUnconfirmedMatches 朋友过期退款从「每条退全部 N 格」改为「每条退 1 格」（原 K 条过期退 K×N，最多 5× 超退）；恋人仍退全部预扣。②【修·退款去重】refundInTx 去重优先 dedupeKey（过期退款带轮次锚点 expire:matchId:createdAt），复用同一 Match 行跨轮的合法退款不再被上一轮流水错误挡掉。③【非 bug·确认】confirm 后离开 searching——轮次制下正确（匹配即 matched，确认后 relationship，下轮可经 startMatch 重进池；朋友多确认有 notIn['relationship'] 守卫不降级），无需改。④【修·ranker 量纲】orchestrator 阈值门改用 fusion 分（τ=60 是按 fusion 分布校准的），ranker 分是稀有事件概率混合（压缩在 ~10-35），原来两者比同一 τ 会把非会员对几乎全丢；ranker 仍负责排序。仅在加载训练好的 ranker 时生效（当前 ranker_model_path 为空，无影响）。⑤【修·归因】attribution build_samples 从「按 pair 无时间窗池化」改为 last-touch（每事件归给 shownAt<=event.at 的最近曝光），修跨轮标签污染；仅离线训练路径（P1-2）。⑥【实现·matchBasis】fusion 按用户 matchBasis 重加权组件（双方一致才生效、分歧回退 both、归一化回 0..100），原来声明了却从不读取。⑦【非 bug·确认】global_match「stable 留下未匹配用户」——opus 复现并证明是 Gale-Shapley 稳定性保证的正确行为（更大匹配含阻塞对；8 万实例验证 blocking=0），无需改。验证：API tsc+jest 20/20、matching-ml 冒烟 6/6、matchBasis（questionnaire 压低 profile 组件、分歧/缺省与 both 逐字节一致）+ 归因（安静轮标 0、成功轮标 1）实测。所有 6 项已处理完毕（4 修复 + 2 确认非 bug），无遗留待决项。
- 完成（找 bug 第二轮：补扫 + 复核中断项，opus 复核）：上一轮验证阶段撞额度中断，本轮补扫 admin-web/config-deploy/matching-ml-core 三维度 + 真正复核 social/matching-energy 的中断发现。26 项确认，已修 20：【安全/隐私】① setMatchPreferences 剥离 enhancedModeEnabled/friendEnhancedCells（原可 PUT 偏好白嫖付费增强，绕开 startMatchForUser 的能量预扣）；② shapePost 剥离 metadata（metadata.reports 含举报人 id+理由，泄露给所有人含被举报作者）；③匿名帖评论/点赞通知改用化名 + 点赞 metadata 存不可反推 token（原泄露真实昵称/id，楼主可反解匿名）；④5 处 User.settings 读改写统一走 PrismaService.updateUserSettings 行锁（FOR UPDATE），消除并发丢更新。【能量】⑤ executeMatchJob 每轮结束清 enhancedModeEnabled（增强按轮付费，原付一次后每轮免费强配+重复退款）。【匹配-ml】⑥ romantic 硬门补 per-mode preferredGender 强制（原只看 profile genderPref，用户经偏好设的性别要求被忽略——已实测：偏好冲突→0 对、满足→1 对）。【部署】⑦ Postgres/Redis 从 0.0.0.0 改绑 127.0.0.1（原把 DB + 无鉴权 Redis 暴露公网）；⑧ Dockerfile 去 --accept-data-loss、演示 seed 收进 SEED_DEMO 开关（原每次启动可能毁数据+灌假数据）；⑨ H5/admin API base 修正（H5 混合内容+错端口；admin NEXT_PUBLIC 必须 build ARG，运行时设无效）；⑩补 matching-ml 服务 Dockerfile + compose 接线 AI_PROVIDER（原部署 compose 没 matching-ml、没 AI env，P0-1 在生产静默回退本地打分）；⑪ set-weekly-schedule.sh 端口 3000→3001+前缀。【social/admin】⑫ chat poll 复合游标(不丢同毫秒消息)+nudge 置顶；⑬举报评论楼中楼 reply-to-reply 重挂顶层(原入库计数通知却不渲染)；⑭推荐流墙卡 seededShuffle 按天稳定(原每请求重洗致分页重复/漏卡)；⑮ admin 登录失败可见(401 拦截放行登录请求)、⑯赞助商停用改 isActive:false(原硬删账号)、⑰手动匹配触发恋人+朋友双模式、⑱用户搜索去抖、⑲ads ?status= 深链筛选生效。验证：API tsc+jest 20/20、admin tsc、H5 vite build、matching-ml 冒烟全过、E2E 4→2 对、性别偏好硬门双向实测。**留给用户决定的 6 项**（设计/风险，未改）：friend 增强 48h 过期退款按格数×匹配数超退、refundInTx 按 matchId 去重会挡住跨轮复用 Match 的合法退款（两者都在默认关闭的 energy.refundOnExpire 后面）、确认朋友后 matchState 离开 searching（轮次制 vs 连续搜索是产品决策）、orchestrator ranker 分数量纲（仅加载训练好的 ranker 时触发，属 P0-4 校准）、global_match 稳定匹配在某场景的边界（有测试保护、repro 看不全，盲改风险高）、attribution 无时间窗归因（已知设计取舍）、schemas.matchBasis 声明未接线（功能缺口）。
- 完成（第一轮找 bug + 代码整理）：8 路子系统多代理扫描 + 逐条对抗复核（3 视角），确认并修复 4 项（一批 matching-energy 声明经复核判为误报）：①【严重】上传接口存储型 XSS——imageFileFilter 只校验 mimetype(客户端可控)，存盘扩展名却取自 originalname，伪造 image/png+filename=x.svg 绕过 → 落盘 .svg 被当 svg+xml 提供在 API 源执行；改为扩展名一律由 mimetype→ext 白名单派生。②③【严重】H5 存储型 XSS——avatarUrl/帖子图/realPhotos 是 @IsString 自由串，原样插进 src="${url}"，escapeHtml 不转义引号 → x" onerror="偷 localStorage.cl_token；加 window.safeUrl(拦 javascript:/vbscript:/file:/非图片 data:，转义属性破坏字符)+window.safeCssUrl(剥离引号/括号防 url() 闭合)，扫掉 square/match/chat/couple/addfriend/profile/milestone/ads 全部 src 注入点 + couple 封面/profile 背景图，照片查看器 onclick 改用 this.src 不拼用户串。④【中】H5 换账号 cleanupUserState 漏重置 S.enhanced/energy/matchBasis → 新用户继承前账号增强开关被扣费；补齐重置。验证：safeUrl/safeCssUrl 对攻击向量逐一实测拦截、正常 URL 放行；API tsc + jest 20/20、H5 vite build 通过。
- 完成（整理）：根目录 6 份设计/审计文档(PRD/DESIGN-DUAL-MODE/BUSINESS-LOGIC-AUDIT/FIXLOG/FRONTEND-LOGIC-ISSUES/H5-GAPS)归入 docs/，根目录只留 README/BACKLOG/SCHEDULING/CLAUDE；删 apps/website/_preview-*.html + _vtest.html 设计草稿(含 6.7MB base64 dump，正式站 index.html 不引用)；删无引用的 stub-match-model.provider.ts。三批干净提交。
- 完成（匹配闭环，BACKLOG P0-1/P0-2/P0-5 全部落地并实弹验证）：①P0-1 接入 matching-ml：ai-match-model.provider.example.ts 升级为正式 AIMatchModelProvider（超时/AbortController、响应结构校验、错误消息去 PII），matching.module.ts 改 env 工厂选择（MATCH_MODEL=ai|scoring，缺省设了 AI_PROVIDER_URL 即 ai，一行回滚）；②P0-2 埋点：MatchExposure（补 matchJobId 列 + (matchJobId,matchId) 唯一键防 Bull 重试重复曝光）+ MatchBehaviorEvent 进 schema 并 db push；曝光在 executeMatchJob 逐对事务提交后落库（featureSnapshot 冻结自模型 metadata），confirmed/rejected(临时解除)/dissolved(永久解除)/firstMessage/message 挂服务端权威动作处（matching.service / chat.service），viewed/openedProfile 走新端点 POST /matching/feedback/events（DTO 白名单 + 归属校验 + 按 (matchId,actorId,type) 去重防刷库）；③P0-5：/match 加 Bearer 鉴权（MATCH_API_KEY，constant-time），空 key 仅允许回环地址，非回环+空 key 拒绝启动（fail-closed）；④环境：装 Python 3.12 + matching-ml venv，Docker 起 postgres/redis。验证：tsc 过、jest 20/20、matching-ml 冒烟全过（含 ranker 学习信号）、鉴权 401/401/200、E2E 实弹 romantic+friend 各 4 候选→2 对（metadata 含算法/宪法版本/28 特征快照/中文理由）、确认→RELATIONSHIP_ROMANTIC、8 类行为事件全落库、UTF-8 hex 往返无损、伪造 confirmed 被 400、重复事件 accepted:0。对抗性审查（5 维度×3 视角，受限额部分中断）确认 3 项已全修复：事件刷库、fail-open 默认配置、错误回显 PII。
- 完成（仓库对齐）：删除 unipia 根目录 api/ h5/ admin-web/ 部署快照（apps/ 为唯一源码真源），origin(campus-love) 标记弃用。
- 完成（上半日，双向同步）：本地 ↔ unipia 双向同步。①把 .env / apps/api/.env 移出版本库（含真实密钥，unipia 政策也禁止），.gitignore 补 .env.* 与 .claude/；②7/4 遗留的全部工作区改动分 6 批提交（api 广告商业化+审核+角色 101 文件 / admin-web 三角色后台 / h5 / ios 改名+web+website / 根文档 / 同步 unipia）；③merge unipia/main（--allow-unrelated-histories）拉下 matching-ml 匹配模型服务（含 7/8–7/10 十次迭代）、BACKLOG.md、SCHEDULING.md、scripts/set-weekly-schedule.sh；④推送合并后的 main 到 unipia（内容为两边超集，未删 unipia 的 api/ h5/ admin-web/ 部署快照目录）。origin(campus-love) 未推送。
- 待办 / 明日计划：服务器部署 matching-ml + 配 AI_PROVIDER_URL/MATCH_API_KEY（P0-1 的部署半）；正式切周日公布 cron（P0-3，运营操作，scripts/set-weekly-schedule.sh）；H5 接 viewed/openedProfile 上报（P1-6）；攒数据后每周训 ranker（P1-2）。本地 e2e.r1–r4@test.local 测试用户留在开发库可复用。

### 2026-07-12
- 完成：将 github.com/pkupig/unipia 添加为远程（`unipia`）并与本地全面对比。结论：unipia 是服务器部署快照仓库（api/ 为 Docker 导出、h5 为构建产物），核心增量是本地完全没有的 matching-ml 匹配模型服务（7/4 导入 + 7/8–7/10 共 10 次迭代提交）及 BACKLOG.md / SCHEDULING.md / scripts/set-weekly-schedule.sh；本地是 unipia 的超集（广告商业化 6 模型 + 三角色后台 + Unimatcha 品牌名，均未提交）。后端已有 ai-match-model.provider.example.ts 契约文件（matching.module 当前绑定 ScoringMatchModelProvider），埋点表 MatchExposure/MatchBehaviorEvent 尚未建（unipia BACKLOG P0-2）。
- 待办 / 明日计划：等用户指示同步方式（预计：把 matching-ml + 文档搬入本地 monorepo；按 BACKLOG P0 接入 provider、建埋点表）。7/4 遗留：分批 commit 工作区改动。

### 2026-07-04
- 完成：广场/校园墙管理 + 举报处理全部落地并验证（团队全量、学生会本校隔离 403、举报队列、下架/恢复/清除举报、用户反馈处理、/moderation 双视角 UI）。对抗性审查（4 维度 × 逐条反驳复核）确认 14 个问题并全部修复：①【严重】CPM/CPC 结算不封顶——普通用户可刷点击让平台按虚增消耗给学校分成真金白银 → 事件路径消耗原子增量+展示封顶、结算基数按实收预算封顶（多校按比例折算），已 E2E 实测（100 次刷点击→消耗精确封顶 1000 分、分成 100 分）；②BUYOUT 流水缺失 → accrueBuyoutSpend 按天摊销（调度器 tick + 到期补齐 + 启动追赶）；③过去 startDate 计入锁价 → 创建/提交双重校验；④并发批次 spend 丢更新 → 原子增量；⑤【严重】admin-matching / admin-questionnaire 控制器裸奔无 RolesGuard（商家可读全平台用户/改问卷）→ 补 SUPER/TEAM 守卫；⑥商家无法拉学校列表 → 瘦身响应含生效单价；⑦前端字段对账 4 处（earnings balance、报表 gross/keep/withdrawal 字段、学生会待审角标、举报用户昵称）；⑧H5 事件批 >100 被 400 整批丢弃 → 分片上报 + 4xx 不重试；⑨H5 换账号不重置广告会话 → token 变更自动重置。收入报表窗口口径（流水按投放日、分成按结算日）属计提/结算时点差异，保留现状。
- 待办 / 明日计划：整理提交全部工作区改动（建议按 后端/admin-web/h5/docs 分批 commit）；商家充值→真实支付渠道、广告素材图片上传（目前 URL 输入）留待下阶段。

### 2026-07-03
- 完成：创建 CLAUDE.md，建立每日日志制度。启动 Admin 平台三角色重写：完成业务设计文档 [docs/ADMIN-REDESIGN.md](docs/ADMIN-REDESIGN.md)（角色权限、广告混合计价 BUYOUT/CPM/CPC、分级审核、双档分成、赞助发放与提现流程）；Prisma 新增 School / AdCampaign / AdPlacement / AdDailyStat / SchoolLedgerEntry / WithdrawalRequest 模型并完成 db push + 存量 schoolId（学校名→School.id）数据迁移；新增 RolesGuard/@Roles 鉴权层；schools/ads/finance 三个后端模块、admin-web 三角色前端重写、H5 广场广告注入并行实现中。本机无 Node，安装了便携版 Node 20 到 C:\Users\DingM\.claude\tools。
- 完成（续）：后端（schools/ads/finance 模块 + admin 角色化改造 + 种子数据）与前端（三角色导航/登录/仪表盘 + 学校/账号/广告/财务/收益/赞助商全部页面）+ H5 广告注入全部落地并编译通过。已验证：三角色登录与范围隔离、学生会审核→团队确认收款→SCHEDULED 全流程、BUYOUT 报价（2校×3天×20000=120000 分）、自拉商家跨校投放 403、提现审批→打款→负数入账→余额守恒、H5 信息流 SPONSORED 大卡展示与曝光计费闭环（曝光 2→3、消耗 10→15 分）。补建 /sponsors 页面（工作流分工遗漏）；修正 dashboard 取值与 placements 校名显示。
- 进行中：追加需求——广场/校园墙帖子管理（学生会本校、团队全量，下架/恢复）+ 举报处理（被举报帖子队列 + 用户反馈处理），后端与 /moderation 页面并行实现中。
- 待办 / 明日计划：广场管理验收；全量代码对抗性审查并修复；整理提交工作区改动（注意 matching 测试文案不一致已拆分为独立任务）。
