{
  "name": "unimatcha",
  "version": "2.0.0",
  "description": "大学生长期恋爱匹配平台 — monorepo（api / admin-web / h5 / ios）",
  "private": true,
  "scripts": {
    "dev:api": "npm --prefix apps/api run start:dev",
    "dev:admin": "npm --prefix apps/admin-web run dev",
    "dev:h5": "npm --prefix apps/h5 run dev",
    "build:api": "npm --prefix apps/api run build",
    "build:admin": "npm --prefix apps/admin-web run build",
    "build:h5": "npm --prefix apps/h5 run build",
    "install:all": "npm --prefix apps/api install && npm --prefix apps/admin-web install && npm --prefix apps/h5 install",
    "up": "docker compose up -d --build",
    "down": "docker compose down"
  }
}
